# OneDrive/SharePoint Storage Support — Full Plan

> Related: [connectors-additional-providers-support.md](connectors-additional-providers-support.md) — that plan's Phase 1a/1b/1c (workspace migration, permission-grant, Inngest abstraction, enum/field renames) are **already implemented** in the current codebase (`IConnectorMigrationAdapter`, `PERSONAL`/`SHARED` enum, `connectorPermissionId`). This document is the current, actionable plan for the remaining work: closing the last abstraction gap (Phase 0) and then building OneDrive/SharePoint itself (Phases 1–6).
>
> **File renamed** from `connector-abstraction-document-lifecycle.md` to `connector-microsoft-impl.md` on 2026-07-26 (pure rename, no content change at the time).
>
> **OPEN — Google-only feature gaps discovered while porting file-listing/upload/download to OneDrive (2026-08-05), not yet closed for OneDrive:**
> 1. **Activity/security badges (risk, stale, sensitive, attention)** — `GoogleDriveConnector.calculateBadges()` and the 90-day activity-staleness check have no Microsoft Graph equivalent (Graph has no `driveactivity.query`-style API). This was already explicitly deferred out-of-scope back in Phase 0's original plan. OneDrive files in Engagement > Files simply show no badges — documented gap, not a bug.
> 2. **Explicit per-file Graph-permission-grant visibility check** — Google's `listFiles()` non-lead permission filter has two layers: (a) folder-tree/role structural rule (eng_admin sees General+Confidential, eng_member sees General only) and (b) a secondary check for files explicitly shared outside that structure (Drive `permissions` array / ownership). For OneDrive, only layer (a) was ported (via `IConnectorStorageAdapter` folder-hierarchy walks) — layer (b) was explicitly skipped to avoid N extra Graph `/permissions` calls per folder listing. Practical effect: a OneDrive file individually shared with a non-lead engagement member from *outside* their normal General/Confidential folder tree will not surface for them, unlike Google. Rare edge case, not the common path, but a real behavior gap worth closing later if it's ever hit in practice.
> 3. **Cross-engagement folder copy doesn't individually index nested items for OneDrive** — Google's `recursiveCopy` manually walks a folder tree and returns every copied item (folders + files) for per-item indexing. Graph's `POST /items/{id}/copy` is natively recursive server-side but only returns the copied *root* item in its completion status — the nested tree isn't enumerated in the response. `recursiveCopyOneDrive` (lib/connectors/adapters/onedrive-file-ops.ts) therefore only indexes the root of a cross-engagement folder copy; files nested inside a copied OneDrive folder won't be individually searchable/indexed until some other trigger (e.g. opening them) re-indexes them. Real gap, not yet closed — would need a follow-up Graph `/children` walk post-copy to enumerate and index the full tree.
> 4. **Version history (revisionId) and Drive-activity-feed are Google-only** — `app/api/documents/versions/route.ts` (file revision list) and `app/api/documents/activity/route.ts` (activity feed) both now return an empty/`unsupported: true` response for non-Google connectors rather than erroring, but no Graph-based version-history implementation exists yet. Graph does expose item versions via a different API (`/items/{id}/versions`) — real feature gap, not yet built.
> 5. **Custom Drive file properties (`appProperties`) aren't synced for OneDrive** — `SearchService.indexFile`'s org/client/project-id tagging on the storage file itself (`updateAppProperties`) is Google-only; Graph has no equivalent per-item key-value property store. Not a functional gap in practice — this metadata is redundant with what's already in our own DB — but noted for completeness.
> 6. **TODO (medium) — "Import from OneDrive/SharePoint" file picker not implemented.** Engagement > Files' "New File / Folder" menu has an "Import from Google Drive" option (firm admins only) backed by Google's official Picker JS widget + a dedicated import API route + `google-drive-import-dialog.tsx`. As of 2026-08-05 this menu item is simply **hidden for OneDrive-connected clients** (`isOneDriveClient` check in `engagement-file-list.tsx`) rather than shown broken. A OneDrive equivalent would need: the Microsoft OneDrive File Picker SDK (`OneDrive.open(...)`, a comparable embeddable JS widget), a new import API route mirroring `app/api/connectors/google-drive/import/route.ts`, and a new dialog component mirroring `google-drive-import-dialog.tsx`. Deliberately parked rather than attempted ad hoc — real scope, not a quick swap.
> 7. **TODO (medium) — Non-PDF-native file preview is incomplete/wrong for both providers, not just OneDrive.** Combines two related reports:
>    - **Wide spreadsheet preview renders portrait/clipped.** `getPreviewableContent` falls back to PDF conversion for any non-inline-viewable mimeType (`IConnectorContentAdapter.getPreviewableContent` → `format: 'pdf'`). For `.xlsx` (OneDrive: Graph's `content?format=pdf`; Google: Drive's `exportLinks['application/pdf']` for uploaded Office files) the PDF export uses the file's own saved print/page-setup orientation — there's no per-request "force landscape" param on either API. Confirmed this is **not OneDrive-specific**: a Google Sheet has the identical issue since Sheets-to-PDF export is also page-based. Reported 2026-08-05 against a `.xlsx` on a OneDrive-connected client (`Apex_Softcell_Internal_Audit_Report_ISO27001-2022.xlsx`, EQ-Seer engagement).
>    - **`.txt`/`.csv` preview unsupported** (`docs/todo-bugs.md` #3, Engagement > Files section) — same root cause: these mimes aren't in `INLINE_VIEWABLE_MIME_PREFIXES` and have no meaningful PDF conversion path either, so preview fails/is blank for both providers.
>    - **Proposed real fix (not yet scoped/started): native tabular/text preview instead of PDF conversion**, replacing the PDF fallback path for spreadsheet and plain-text mimetypes specifically:
>      - `.xlsx`/`.csv` (uploaded Office files, both Google Drive and OneDrive — Drive doesn't server-side-export uploaded `.xlsx` to structured data either, so this is the same raw-bytes case as OneDrive): fetch raw bytes via `getRenderableContent(id, 'native')`, parse client-side with a spreadsheet-parsing library (e.g. SheetJS), render as a scrollable HTML table with multi-sheet tab support. Orientation stops being a concept — it's just a table.
>      - Native Google Sheets (`.gsheet`): could reuse the same table renderer (Drive can also export Sheets to `.xlsx`/CSV) instead of the current PDF path, for consistency — or leave Google Sheets on the existing PDF path and only add the new renderer for uploaded `.xlsx`/`.csv`; open decision.
>      - `.txt`: trivial — just render the fetched text content directly (no parsing library needed), already the missing piece for bug #3.
>    - **Scope**: new preview-pane component + client-side parsing dependency (bundle-size tradeoff to weigh) + wiring into the existing preview route's format-decision logic. Discussed with user 2026-08-05, deliberately not started pending a scoping/cost decision.
> 8. **Platform limitation (not a gap to close) — no "Microsoft Form" option in the "New file" menu.** Unlike Word/Excel/PowerPoint, Microsoft Forms has **no Graph API for creation** — a Form is not a driveItem (no file representation in OneDrive/SharePoint at all), only a generic web landing page exists (`forms.office.com/Pages/DesignPageV2.aspx`) with no way to pre-create a blank form, return a file/id, or attach it to an engagement folder the way Google Form's `files.create` does. Researched and confirmed 2026-08-05 (Microsoft Learn Q&A threads, no supported public API; the only "API" is an unsupported, reverse-engineered internal endpoint). Deliberately **not added** to the menu — a link-out to the generic Forms homepage would set an inconsistent expectation vs. every other item in that menu, which all create-and-attach a file in the current folder.
> 9. **DONE (2026-08-06) — Auto-complete Step 2 entirely for personal Microsoft accounts, no click required.** A personal Microsoft account (MSA — `@outlook.com`, `@hotmail.com`, etc.) can never have a SharePoint site, so requiring the user to click "Choose folder" for a foregone conclusion was unnecessary friction (per explicit user correction 2026-08-06: "why have the user click the button? when there is no decision to be made"). Detected by decoding the `tid` (tenant ID) claim from the OAuth `id_token` at callback time (`app/api/connectors/onedrive/callback/route.ts`'s `isPersonalMicrosoftAccount()`) — all personal Microsoft accounts share the fixed, well-known tenant ID `9188040d-6c67-4c5b-b112-36a304b66dad`; any other `tid` is a work/school (Entra ID) tenant. No extra Graph call needed — `openid` was already in `CONNECT_SCOPES`, so the `id_token` was already being returned. The flag is persisted as `Connector.settings.isPersonalAccount` (via `OneDriveConnector.storeConnection`'s new `isPersonalAccount` param, same pattern as the existing `accountEmail` field), surfaced through the `action=status` endpoint (note: `firm-drive-section.tsx`'s `loadOneDriveStatus` initially dropped this field when reconstructing its local status object field-by-field despite the type declaring it — a real bug caught via live testing, fixed same day), and threaded into `OneDriveWorkspaceRoot` as a new `isPersonalAccount` prop. A `useEffect` (ref-guarded to fire once per connection) auto-invokes `createPersonalFolder()` the moment the component renders with `isPersonalAccount === true` and no `rootFolderId` yet — no button, no dialog, no click. The "No workspace folder" row renders an inline spinner ("Setting up your workspace folder in OneDrive…") instead of a "Choose folder" button for this case, which naturally clears once `onUpdated()` refetches and `rootFolderId` is set. **The "Migrate" button is hidden entirely for personal accounts once the folder exists** — per explicit user follow-up ("I dont think there is any need for Migrate button as well, that will remove some more of the complicated features"): a personal MSA can never migrate to a SharePoint site (that option was always dead for them), and re-creating the auto-generated folder isn't a capability worth the added complexity/decision surface — if ever needed, disconnect/reconnect re-triggers the same auto-create flow. Work/school accounts keep the Migrate button unchanged (real choice: Personal ↔ a different SharePoint site). Fails closed: an undecodable/missing `id_token` defaults to `isPersonalAccount = false`, preserving today's manual two-option Step-2 screen AND the Migrate button for work/school accounts.
> 10. **DONE (2026-08-06) — Same auto-skip concept applied to Google Drive's My Drive/Shared Drive choice.** A personal Gmail account can never have a Google Shared Drive (only Google Workspace/Cloud org accounts can) — `GoogleDriveWorkspaceRoot`'s My Drive/Shared Drive picker previously showed the same two-button choice to everyone, with no Workspace-vs-personal detection.
>     - **Scope change required (unlike the free OneDrive fix)**: added `openid` to the Google OAuth scope list (`app/api/connectors/google-drive/route.ts` — `drive.file`/`drive.appdata`/`userinfo.email`/`userinfo.profile` unchanged) so an `id_token` is now issued. Detection decodes the `hd` (hosted domain) claim from that `id_token` — present (any value) for Google Workspace/Cloud org accounts, absent for personal `@gmail.com` accounts (`app/api/connectors/google-drive/callback/route.ts`'s new `isPersonalGoogleAccount()`, mirrors OneDrive's `isPersonalMicrosoftAccount()` JWT-decode pattern exactly). Did **not** switch to the OIDC userinfo endpoint — decoding the already-fetched `id_token` was simpler and needed no new network call.
>     - Persisted as `Connector.settings.isPersonalAccount` via a new `isPersonalAccount` param on `GoogleDriveConnector.storeConnection` (same shape as OneDrive's), surfaced through the `action=status` endpoint, and threaded into `GoogleDriveWorkspaceRoot` as a new `isPersonalAccount` prop.
>     - **Caught the exact same field-drop bug pre-emptively**: `firm-drive-section.tsx`'s Google `loadStatus` (the Google counterpart to the OneDrive `loadOneDriveStatus` bug found earlier the same day) also reconstructed its local status object field-by-field — fixed by adding `isPersonalAccount` to that constructor before it could ship broken.
>     - A ref-guarded `useEffect` auto-invokes the already-existing `autoCreateMyDriveFolder()` (previously only reachable via `startMyDriveFlow()`'s "no existing folder" branch, itself only reachable by clicking "Choose folder") the moment the component renders for a confirmed-personal account — no button, no click. The "No workspace folder" row shows an inline spinner ("Setting up your workspace folder in My Drive…") instead of "Choose folder" for this case. "Migrate" is hidden entirely once the folder exists, mirroring item 9's OneDrive Migrate-removal exactly (same rationale: a personal Gmail account can never migrate to a Shared Drive, and the auto-created folder stays in My Drive permanently).
>     - Confirmed the unused/dead `components/connectors/google-drive-connector-tab.tsx` (a separate `GoogleDriveWorkspaceRoot` call site with no import references anywhere in the codebase) was intentionally left untouched — not wired to anything, not worth updating.
>     - Test coverage: none added for this specific UI flow (matches OneDrive's `onedrive-workspace-root.tsx`, which also has no dedicated component test) — `tsc --noEmit` clean, existing `storeConnection` test suites (already updated for item 11's targetConnectorId change) still pass with the added `isPersonalAccount` param.
> 12. **IMPLEMENTED, best-effort (2026-08-06) — `setCopyRestricted` no-op meant EC/EV/Viewer roles could bypass "Allow Download: off" on OneDrive connections.** User flagged this reviewing the deliverable Settings tab (Contributor/Reviewer "Allow Download" toggles) — asked whether the regrant flow (`app/api/projects/[projectId]/documents/[documentId]/sharing/regrant/route.ts`) enforces the same download restriction for OneDrive as it does for Google Drive. The regrant flow itself was already fully provider-agnostic and correct — but `IConnectorContentAdapter.setCopyRestricted`'s OneDrive implementation was a **silent no-op**, so a "download disabled" file remained downloadable via OneDrive's own native UI for OneDrive-connected engagements. Google Drive was (and is) unaffected.
>     - **Research (two rounds) found**: Graph's direct-user `/invite` permission (used for all EC/EV grants) has no per-permission or per-item download-block property; `preventsDownload` exists only on `sharingLink`. Layering a restrictive `createLink` link on top of an existing `/invite` grant does **not** work — SharePoint/OneDrive takes the union/least-restrictive of all grants a user holds on an item. The real mechanism is `createLink` with **`type: 'blocksDownload'`** (not a `preventsDownload` boolean on `view`/`edit` — that's a read-only response field, not a request param), which **requires Graph's beta API** — v1.0's `createLink` has no `recipients` field to scope the link to one user. Microsoft's own policy: "APIs under `/beta` ... use in production applications is not supported." Additionally, `blocksDownload` silently degrades based on tenant SharePoint/OneDrive license and file type (some formats excluded even when the API call succeeds).
>     - **Decision**: given no live Graph tenant to smoke-test against, implemented this as a **best-effort, self-verifying** grant path rather than either (a) leaving it a silent no-op or (b) blindly trusting an unsupported beta call. `grantFilePermission`'s `opts` gained a new `preventDownload?: boolean` field (interface-level, `lib/connectors/types.ts`) — Google's implementation ignores it (download-blocking is handled by its existing `setCopyRestricted`, unchanged); OneDrive's implementation (`onedrive-permission-adapter.ts`'s new `grantDownloadBlockedLink`) replaces the normal `/invite` call with `POST {beta}/items/{id}/createLink { type: 'blocksDownload', scope: 'users', recipients: [{email}] }` whenever `preventDownload` is true. The response is checked for `link.preventsDownload === true`; if the beta call fails outright or doesn't confirm the restriction, it logs a `logger.warn` (visible/searchable in server logs) and **falls back to a normal `view` link** so the EC/EV member still gets access rather than being silently locked out — download just isn't confirmed-blocked in that fallback case.
>     - `regrant/route.ts` now tracks whether `setCopyRestricted(true)` was requested across its three branches (PDF-only, Viewer-full-file, EC-persona) into a `copyRestricted` flag, threaded into the single `grantFilePermission(..., { message, preventDownload: copyRestricted })` call — this is what actually triggers the OneDrive `blocksDownload` path instead of the plain invite.
>     - `listFilePermissions` (`onedrive-permission-adapter.ts`) was fixed to also read `grantedToIdentitiesV2` (plural — populated by link-type permissions) alongside the existing `grantedToV2` (singular — populated by `/invite` permissions), so a download-blocked link permission is still matchable by email (needed by regrant's duplicate-grant fallback lookup, which searches `listFilePermissions` results by email).
>     - **Known residual risk, by design, not silently hidden**: this depends on an explicitly-unsupported-for-production Graph beta endpoint and can silently not-enforce on some tenants/file-types even when the API call itself succeeds — that's the nature of the underlying Microsoft feature, not an implementation shortcut. The self-verification + warning-log + safe-fallback pattern is the mitigation given no way to guarantee enforcement ahead of a live-tenant test. Folder-level grants (`grantFolderPermission`, used for eng_admin/eng_member folder access) are unaffected — they stay on the normal `/invite` path, since this gap and fix are specific to per-file EC/EV/Viewer secure-access grants.
>     - **Follow-up once live Graph tenant access is available**: verify against a real OneDrive-for-Business tenant that (1) the native web UI actually suppresses the Download button for a `blocksDownload`-linked recipient, (2) `preventsDownload: true` is reliably present in the response (vs. needing a follow-up call), (3) the app's own authenticated download path (`getRenderableContent`, which uses the connector's own service-level token, not the recipient's) is unaffected by the recipient's link restriction — expected to be fine since that path never goes through the recipient's grant at all.
>     - **RETEST CHECKLIST (2026-08-06) — deliverable Settings tab (Contributor (External) / Reviewer: Allow Download, PDF Only, Apply Watermark) against a live OneDrive-connected engagement, compared to Google Drive.** User asked "are these settings all honored" for Microsoft connectors — answer as of this code (needs live verification, not yet tested against a real tenant):
>       - **PDF Only** — expected honored identically on both providers. Fully provider-agnostic: uses `contentAdapter.getRenderableContent(..., 'pdf')` (Graph's `?format=pdf` content endpoint for OneDrive) then `createFile`/`overwriteFileContent` to upload the exported PDF. No known gap. Retest: toggle PDF Only for a Reviewer role on a OneDrive engagement, confirm the EC/EV recipient's secure-access link opens a PDF, not the native file.
>       - **Apply Watermark** — expected honored identically on both providers. Runs `applyDiagonalWatermark` on PDF bytes in-process (a PDF-manipulation library call, not a Drive/Graph API) before upload — fully provider-independent. No known gap. Retest: toggle Apply Watermark alongside PDF Only, confirm the diagonal firm-name watermark appears in the PDF the EC/EV recipient receives, same as on a Google-connected engagement.
>       - **Allow Download** — **best-effort only for OneDrive**, per item 12 above. Real enforcement (native OneDrive download button suppressed) only when Graph's beta `createLink(blocksDownload)` call succeeds AND the response confirms `preventsDownload: true` for that specific tenant/file-type; otherwise silently falls back to a normal (downloadable) view link with a `logger.warn` in server logs — access is preserved, but the deliverable's "off" setting may not actually be enforced. **This is the one setting that genuinely needs a live-tenant retest** to know its real-world enforcement rate: (1) toggle Allow Download off for Contributor/Reviewer on a real OneDrive-for-Business-connected engagement, (2) have an EC/EV member open the secure-access link, (3) confirm whether OneDrive's own web UI actually hides/blocks the Download option, (4) check server logs for the `[onedrive-permission-adapter] createLink(blocksDownload) ...` warning to see whether the beta call succeeded/was confirmed or silently fell back. Google Drive is unaffected — `copyRequiresWriterPermission` is a real, always-enforced Drive property, so Allow Download is fully honored there today.
> 11. **DONE (2026-08-06) — Fixed: "Add new connection" silently merged into an existing connector for the same account instead of creating an independent row.** Live bug: connecting `deepak@firmaone.com` a second time (intending a new, independently-configured connector — e.g. Personal-mode for Client A vs. a different SharePoint site for Client B) instead found and updated the *existing* connector's tokens/`updatedAt` in place — no new row was created, and the user's stated intent ("Add new connection", not "Reconnect") was silently ignored. Root cause: `storeConnection` (both `lib/google-drive-connector.ts` and `lib/connectors/onedrive-connector.ts`) deduped by `findFirst({ type, userId, externalAccountId })` unconditionally — same external account always collapsed to the same row, regardless of caller intent. The UI already distinguished intent correctly (`handleConnect`/`handleOneDriveConnect` for "Add new" pass no `replaceConnectorId`; `handleReconnect`/`handleOneDriveReconnect` pass the target connector's own `id` as `replaceConnectorId`, threaded through OAuth `state` to the callback routes) — the backend just never listened to it.
>     - **Fix**: added `Connector.slug` (new required, globally-unique column — `prisma/schema.prisma`, migration `20260805193102_add_connector_slug`) as an immutable identity anchor generated once at creation (`lib/slug-utils.ts`'s new `generateConnectorSlug()`, following the same pattern as `Firm`/`Client`/`Engagement` slugs), unrelated to the freely-editable `name`. The migration backfills a random slug for the 3 pre-existing rows via SQL (`UPDATE ... SET slug = 'conn-' || substr(md5(random()::text || ...), 1, 10)`) before enforcing `NOT NULL UNIQUE`, and drops the old `connectors_type_userId_externalAccountId_key` unique constraint (replaced with a plain, non-unique index on the same columns — still useful for lookups, no longer used to decide identity).
>     - `storeConnection` on both connectors now takes a new `targetConnectorId?: string` parameter, threaded straight from the already-decoded `replaceConnectorId` in both callback routes (`app/api/connectors/google-drive/callback/route.ts`, `app/api/connectors/onedrive/callback/route.ts` — one-line change each, the value was already in scope right next to the existing `storeConnection(...)` call). New behavior: `targetConnectorId` set → `prisma.connector.update({ where: { id: targetConnectorId } })` directly (no account lookup at all — strictly more precise than the old `findFirst`, since it can never accidentally hit a different row for the same account); `targetConnectorId` unset → **always** `prisma.connector.create(...)` with a freshly generated `slug`, no matter how many connectors already exist for that account. The `findFirst`-by-account call was removed entirely, closing the gap.
>     - **Bonus fix caught in review**: `GoogleDriveConnector.importStructureFromDrive`'s per-discovered-org clone callback (`lib/google-drive-connector.ts` ~line 711-745, used by the legacy Pockett-folder-structure import flow) called `storeConnection` with no `clientId`/target and relied on the old account-based `findFirst` to "happen to" create separate rows per org — in practice, with the *old* buggy logic, importing 2+ orgs from the same Drive account would have silently re-found and overwritten the *same* connector row on every org after the first (a second latent bug, now also fixed as a side effect of removing the account-based lookup — confirmed via `pockett-structure.service.ts`'s own idempotency guard that this callback is only invoked once per newly-discovered org, so no duplicate-row risk from the fix itself).
> 13. **DONE (2026-08-07) — Fixed: reconnect silently reset an already-configured SharePoint connector back to Personal, orphaning its stored root folder.** Live bug: a SharePoint-backed OneDrive connector (`workspaceRootLocation: SHARED`, pointed at the `Firma` site) stopped showing its workspace folder name after a disconnect/reconnect cycle. User's own diagnosis (correcting my initial wrong assumption about a stale-token issue): "this connector is not on me/drive its on the Sharepoint, like the friendly name also suggests." Traced via a newly-added `logger.warn`/`logger.error` in `getFolderName`'s previously-silent catch (`lib/connectors/adapters/onedrive-adapter.ts`) and in `action=status`'s handler (`app/api/connectors/onedrive/route.ts`) — server logs showed a 404 `itemNotFound` resolving the stored `rootFolderId` against `/me/drive` instead of `/sites/{id}/drive`. Root cause: `OneDriveConnector.storeConnection`'s shared `updateData` object (used by both the create and reconnect/`targetConnectorId` paths) unconditionally wrote `workspaceRootLocation` from the `mode` param — but the OAuth callback always calls `storeConnection` with `mode` defaulted to `'personal'` (it has no way to know the connector's real Shared/SharePoint state at reconnect time; that's only set later via the site-selection flow). Every reconnect therefore silently clobbered an already-SharePoint-configured connector back to `PERSONAL`, while its `rootFolderId` kept pointing at the SharePoint drive — the two went out of sync, producing the 404. **Fix**: removed `workspaceRootLocation`/`workspaceRootSharedStorageId`/`workspaceRootSharedStorageName` from the shared `updateData` object entirely — these fields are now only ever set in the `create` path (a brand-new connector has no prior state to preserve/protect). Two regression tests added to `lib/connectors/onedrive-connector.store-connection.test.ts` (10 tests total, all passing) asserting the update path's `data` object never contains these three keys, and the create path always does.
> 14. **DONE (2026-08-07) — Added a "generic Open SharePoint site" link for `SHARED`/SharePoint OneDrive connectors — previously the Open button only existed for `PERSONAL`.** Follow-up to item 13: once the reconnect-clobber bug was fixed, the next reported gap was that SharePoint-backed connectors (`workspaceRootLocation: SHARED`) never showed an "Open" button at all, unlike both Personal OneDrive and every Google Drive connector. Root cause: `onedrive-workspace-root.tsx`'s `driveUrl` only ever constructed a URL for the `PERSONAL` case (`onedrive.live.com/?id=...`, a documented, client-constructible redirect pattern) — no equivalent existed for SharePoint. Researched (sub-agent) whether a SharePoint URL could be reliably constructed client-side from the stored composite site id (`{hostname},{site-collection-guid},{site-guid}`) plus a driveItem id: **confirmed not possible** — Graph's `driveItem.webUrl` is the only supported, documented way to get a browser-openable link, and it's read-only/server-computed (cannot be built from the composite id format). User then proposed a simpler alternative — link to the SharePoint **site's** document library home instead of the exact folder — which doesn't need a live per-render Graph call, since a site's own `webUrl` is stable and available in the site-search response Graph already returns during site selection.
>     - **Fix**: added a new `Connector.workspaceRootSharedStorageWebUrl` column (migration `20260807000000_add_connector_shared_storage_weburl`), following the exact same shape as the existing `workspaceRootSharedStorageId`/`Name` fields. `app/api/connectors/onedrive/sites/route.ts`'s POST (site-selection) handler already fetches the drive root (`GET /sites/{siteId}/drive/root?$select=id`) to resolve the workspace's parent folder id — extended that same call to `$select=id,webUrl` (no extra Graph round-trip) and persists `driveRootData.webUrl` alongside the existing `workspaceRootSharedStorageId`/`Name` write.
>     - **Refined 2026-08-07 (same day)**: initially used the *site's* `webUrl` (`GET /sites/{siteId}?$select=webUrl`), which resolves to the SharePoint site's generic landing page (`.../SitePages/Home.aspx`). User asked for the Open link to land directly on documents instead — switched to the *drive root's* `webUrl` (from the already-being-fetched `/drive/root` call), which Graph returns as the document library's own URL (`.../Shared%20Documents/Forms/AllItems.aspx`), matching Google Drive's Open behavior of landing straight on file listings. Removed the separate site-`webUrl` fetch entirely since the drive-root call already covered it.
>     - Threaded the new field through the existing plumbing end-to-end: `action=status` response (`app/api/connectors/onedrive/route.ts`) → `firm-drive-section.tsx`'s `DriveRoot` type + `loadOneDriveStatus` → `OneDriveWorkspaceRoot`'s new `workspaceRootSharedStorageWebUrl` prop. `driveUrl` now resolves to the stored document-library `webUrl` when `workspaceRootLocation === "SHARED"`, alongside the existing Personal case. The Open button's `aria-label`/tooltip text is now provider-and-location-aware ("Open SharePoint site" vs "Open in OneDrive") rather than hardcoded to OneDrive wording.
>     - **Upgraded 2026-08-07 (same day, final version) — Open now lands on the exact workspace folder, matching Personal OneDrive's behavior, not just the document library home.** User asked how Personal OneDrive's Open manages to land exactly on `_firma_workspace_<random>` while SharePoint's only reached the library root, then asked what it'd take to construct a URL picked up by hand-navigating in the browser (`.../Shared%20Documents/Forms/AllItems.aspx?id=<server-relative-path>&viewid=<guid>`). Analysis: the `id=` param is a server-relative path (constructible from already-known folder names, e.g. `_firma/<workspaceFolderName>`) but the library segment (`Shared Documents`) can be renamed/localized, and `viewid` is a per-library SharePoint-assigned GUID with no derivable value — real drift risk for a small saved fetch. Chose the more robust option instead: fetch `driveItem.webUrl` directly for the just-created/resolved `rootFolderId` (`GET /sites/{siteId}/drive/items/{rootFolderId}?$select=webUrl`, one extra best-effort Graph call right after `adapter.findOrCreateFolder` returns `rootFolderId` in the same POST handler) and persist that instead of the drive-root's `webUrl`. Graph computes this URL server-side with correct encoding/locale/library-name/view, so there's no path-guessing or drift risk. A failure here just logs a warning and leaves the field null (Open unavailable), same fallback pattern as the earlier site-`webUrl` fetch it replaces.
>     - **Known limitation, unchanged**: existing SharePoint connectors selected *before* this fix (or before the exact-folder upgrade) have `workspaceRootSharedStorageWebUrl: null` or pointing at the old library-home URL, and won't show the corrected Open link until re-selected (re-running Step 2's site picker, or disconnect/reconnect + re-select) — not retroactively backfilled, low-priority since the site is already re-selectable in the UI.
>     - Migration is additive-only (nullable column, no backfill, no dropped constraint) — `npx tsc --noEmit` clean after `npx prisma generate`. No behavior change for `PERSONAL` connectors or for `SHARED` connectors that predate this field.
>     - Test coverage: rewrote `app/api/connectors/google-drive/store-connection.test.ts` (previously asserted on the now-removed `findFirst`-by-account behavior) and added the equivalent `lib/connectors/onedrive-connector.store-connection.test.ts` — both cover: update-by-`targetConnectorId`, create-when-unset (even with an existing same-account row), slug generation on create, client-linking on both paths, and a `targetConnectorId` that doesn't resolve throws rather than silently falling through.
>
> 15. **DONE (2026-08-14) — OneDrive regrant (`sharing/regrant/route.ts`, "Authenticate Editor/Reviewer Access") no longer routes through email at all; opens the document directly.** Investigated after a live report: an EC/EV/Viewer role member with a non-Microsoft email clicked "Authenticate Editor Access," the Graph `/invite` permission was granted successfully (confirmed — the same email received edit access when tested as an existing Microsoft account), but **no invitation email arrived** for a genuinely external (non-Microsoft) recipient.
>     - **Root cause (researched, not just inferred)**: Graph's own `sendInvitation: true` notification email is a documented, still-unresolved delivery issue for Azure AD B2B guest invitations sent to recipients with no existing Microsoft account — reproduced live 2026-08-14, and matching multiple independent Microsoft Q&A reports (Jul/Nov 2024, and a closely-related Jan 2026 report attributing it to strict DMARC/SPF/DKIM rejection of Microsoft's system sender domain: https://learn.microsoft.com/en-us/answers/questions/5721785/). Microsoft's own Jan 2026 guidance for the related B2B case: set `sendInvitation: false` and self-send the notification instead — confirms `sendInvitation` and `requireSignIn` are fully independent (documented on the [driveItem: invite](https://learn.microsoft.com/en-us/graph/api/driveitem-invite?view=graph-rest-1.0) reference) — `sendInvitation` only controls Graph's notification email; it has zero effect on the permission grant or the `requireSignIn` authorization/OTP gate.
>     - **Design considered and rejected**: initially built an app-level fallback email (new `lib/email-templates/document-access-granted.ts`, matching the existing `added-to-engagement.ts` pattern) to send alongside Graph's unreliable one. **Rejected on reflection**: this route (`regrant/route.ts`) is a synchronous action triggered by the recipient's own in-browser click — both for the first grant and for "regrant" (re-authenticating after their OTP session expired) — the recipient is already present, not waiting on an inbox. Further, OneDrive's `driveItem:invite` permission has **no default expiration** (confirmed via research — unlike some Google Drive link-sharing types); only the OTP-authenticated *session* is time-boxed (~24h, tenant-default), after which the recipient just needs one more OTP code, not a new invite. An emailed link therefore offers no durability advantage over just clicking the button again — so the email step was dropped entirely rather than added to.
>     - **Iframe considered and rejected**: also considered opening the document inside the app's existing right-pane iframe (the pattern already used for Google Docs/Sheets edit+preview, explicitly designed so "client never sees Google in the UI" per `document-edit-sheet.tsx`'s own comments). **Confirmed technically blocked for Microsoft's sign-in step specifically**: Microsoft Entra ID sends `X-FRAME-OPTIONS: DENY` on all interactive auth pages (sign-in, OTP entry), explicitly documented as non-configurable, with no third-party-app exception (https://learn.microsoft.com/en-us/entra/msal/javascript/browser/iframe-usage) — only *silent* (non-interactive) MSAL flows are supported in an iframe, never an interactive OTP challenge. A framed OTP step would show a browser-native "refused to connect" page, not a working embedded prompt. `window.open` in a new tab (already the existing fallback pattern used elsewhere in the same file) is therefore not just simpler but the only technically viable option for OneDrive's auth step.
>     - **Final implementation**: `onedrive-permission-adapter.ts`'s `grantItemPermission` hardcodes `sendInvitation: false` (was `opts?.notify !== false`) — Graph's unreliable email is never sent for OneDrive grants; `requireSignIn: true` is unchanged, so authorization strength is identical to before. `regrant/route.ts` now resolves the connector's `type` and, for `ONEDRIVE` only, fetches the granted item's `webViewLink` (Graph `driveItem.webUrl`, via the already-provider-agnostic `getFilesMetadata`) and returns it as `documentUrl` in the JSON response — a best-effort fetch; a failure here only logs a warning and omits the field, it does not fail the grant. `ReGrantEditorAccessButton` (`document-edit-sheet.tsx`) branches on the response: if `documentUrl` is present (OneDrive), `window.open`s it immediately in a new tab; otherwise (Google, unchanged) shows the existing "Secure Edit Link Sent — check your inbox" modal, since Google's own OTP delivery genuinely is the email (not just a notification wrapper around a separately-workable link, unlike OneDrive). Also fixed a leftover hardcoded "Google Drive requires a one-time email verification" string in the same route's `message` param to be provider-neutral (missed in the earlier session-wide pass on `secure-access-modal.tsx`), and the button's tooltip ("Click if Google Drive is asking you to sign in" → "Click if your storage provider is asking you to sign in").
>     - **Open question — not resolved by documentation, needs live test**: whether `requireSignIn: true` reliably surfaces email-OTP verification (the lighter, no-password flow — confirmed to be the tenant default for modern Microsoft 365 tenants, force-enabled tenant-wide by Microsoft as of May 2026 per research) versus forcing full Microsoft-account creation, is **tenant-admin-configurable, not guaranteed by the Graph API call itself** — the app has no way to detect or control which a given tenant is on. User's decision: test live rather than keep researching, since only a real non-Microsoft recipient hitting the actual tenant can confirm which path that specific tenant takes.
>     - No `login_hint`/pre-fill query parameter exists for `driveItem.webUrl` (confirmed — that parameter only applies to OAuth `/authorize` endpoints, not this URL shape) and no personalized "redeem" URL is returned by `driveItem:invite` (unlike Azure AD's separate, unrelated `invitation-post` API) — so the recipient's sign-in experience cannot be further streamlined via URL construction; whatever friction exists is inherent to Microsoft's redemption flow.
>     - `npx tsc --noEmit` clean. Not yet tested live against a real non-Microsoft recipient — pending user's manual test.
> 16. **DONE (2026-08-14) — Fixed: attaching a client to a OneDrive/SharePoint connector could fail with "Organization folder not configured; complete Drive setup first," even though the connector's workspace root was already selected and looked fully set up.** Live report: `MS - DS - Outlook - OneDrive` (a Personal OneDrive connector) had a valid `Location` (workspace root folder shown correctly in Firm Settings), yet attaching client DataSentry threw this error, auto-filed support ticket `#TKT-IN0H95` (per item 4's `reportActionFailure`/`ClientLinkedFolderFailedError` machinery working exactly as designed), and left the client linked but with no working Drive folder.
>     - **Root cause**: a connector's Firm-name subfolder (`connector.settings.organizations[firmId].orgFolderId`, distinct from the workspace *root* shown as "Location") is only created by `setupFirmFolder` — normally invoked at connect time by either the OneDrive Personal auto-create flow (`onedrive-workspace-root.tsx`'s `createPersonalFolder` → `action=update-root-folder` in `app/api/connectors/onedrive/route.ts`) or the SharePoint site-selection flow (`app/api/connectors/onedrive/sites/route.ts`). **Both of those routes resolve "which firm" the same, broken way**: `Firm.findFirst({ where: { connectorId } })` (the legacy single-firm-per-connector link, rarely populated for client-level connectors) falling back to an *already-attached client's* `firmId` — but at the exact moment either auto-provisioning flow runs (right after OAuth connect / right after selecting a SharePoint site), **no client has been attached to the connector yet** — that's a separate, later step in this UI (Firm Settings → Clients → attach). So `org` resolves to `null` in both routes, and the `if (org) { ... await setupFirmFolder(...) }` block is silently skipped — the connector ends up with a valid workspace root but no `orgFolderId`, for both OneDrive Personal **and** SharePoint (Shared) alike; not location-specific.
>     - This gap existed from the start but was previously papered over by the very fallback `ensureAppFolderStructure` fix removed in item 13/the 2026-08-07 audit (`org.firmFolderId` used to substitute for a missing per-connector `orgFolderId`) — once that buggy cross-connector fallback was correctly removed, this pre-existing gap started throwing a clean, correct error instead of silently limping along on a potentially-wrong folder ID. Confirmed as a genuine, independent bug, not a regression introduced by item 13's fix — that fix only made an existing gap visible.
>     - **Fix**: `shareConnectorWithClient` (`lib/actions/client.ts`) — the one place that reliably knows both the connector *and* the firm at the moment a client is actually attached — now checks `connector.settings.organizations[firmId]?.orgFolderId` right before calling `ensureAppFolderStructure`, and calls `setupFirmFolder(connectorId, rootFolderId, adapter, client.firmId)` first if it's missing. Self-healing, not a new provisioning path: reuses the exact same `setupFirmFolder` function already called by every other connect-time flow (Google's `route.ts`/`callback/route.ts`, OneDrive's `route.ts`, SharePoint's `sites/route.ts`), via the already-provider-agnostic `getStorageAdapter`.
>     - **No regression risk for Google connectors** (explicitly verified per user question): the new check is purely additive and only fires when `orgFolderId` is genuinely missing for that firm on that connector — for a Google connector where `orgFolderId` was already correctly set by Google's own connect-time flow (which doesn't have this timing gap, since Google's flows were the original, more mature code path), the check finds it already present and the new block is a no-op, falling through to the unchanged `ensureAppFolderStructure` call. `setupFirmFolder` itself is idempotent (`findOrCreateFolder`, not create-unconditionally) and scoped only to the target firm's own folder — no destructive writes, no cross-firm/cross-connector side effects.
>     - `npx tsc --noEmit` clean. Not yet retested live — user is removing and reconnecting the affected connector to confirm end-to-end.
> 17. **DONE (2026-08-14) — Fixed root cause of item 15's grant failure for genuinely new external (non-tenant) recipients: OneDrive/SharePoint's `driveItem: invite` pre-creates the recipient as an Entra ID guest first.** Item 15 made external-grant failures visible (`GraphSharingPolicyError` / "external_sharing_blocked") but didn't fix the underlying cause — this item does.
>     - **Investigation trail**: live A/B test (same document, same code) proved the `sharingFailed` error is specific to *external* recipients — identical `driveItem: invite` call succeeds for an internal-tenant email (`deepak@firmaone.com`), fails for external (`deepak@pockett.io`). Checked and ruled out, in order: (1) duplicate-permission collision (checked via `listFilePermissions` — no existing grant for the email), (2) tenant-wide SharePoint sharing policy (SharePoint admin center → Sharing — confirmed set to "Anyone," the most permissive option, not the cause), (3) site-level sharing policy override (SharePoint admin center → Sites → Active sites → Firma → Sharing — confirmed "New and existing guests," also permissive enough, not the cause). With both SharePoint-layer settings clean, researched further and found the actual cause on Microsoft Learn's `driveItem: invite` reference page: **"New guests can't be invited using app-only access. Existing guests can be invited using app-only requests."** This connector authenticates via a stored service token (not a live interactive user session), which is exactly the case this restriction targets — internal recipients succeed because they're already directory members ("existing"), external recipients with no prior tenant relationship are rejected as "new."
>     - **Fix**: added `preCreateGuestInvitation()` (`onedrive-permission-adapter.ts`) — calls Graph's `POST /invitations` (requires the `User.Invite.All` permission, added to the app registration and granted tenant-wide admin consent 2026-08-14) immediately before `driveItem: invite` inside `grantItemPermission`. This pre-creates the Entra ID guest object for the recipient, so by the time `driveItem: invite` runs, the recipient is already an "existing guest" from Graph's perspective — sidestepping the app-only new-guest restriction entirely. Idempotent/safe to call on every regrant, not just the first: Graph's `/invitations` endpoint doesn't duplicate a guest object for an email that already has one (from this call or any prior manual invite); an "already exists"-shaped error response is treated as success. Failures here are logged but non-fatal — `driveItem: invite` is still attempted afterward regardless, so a `preCreateGuestInvitation` hiccup degrades to the pre-existing item-15 behavior (clear `external_sharing_blocked` error) rather than silently blocking the whole grant.
>     - **Scope note**: only added to the `driveItem: invite` path (`grantItemPermission`), not `grantDownloadBlockedLink` (the separate `createLink`/`blocksDownload` path used for Viewer-role/PDF-only preventDownload grants) — Microsoft's documented "new guest" restriction is specific to `driveItem: invite`; no evidence found that `createLink` has the same limitation, so nothing was speculatively added there.
>     - **Required manual step, done in two parts (the first alone was NOT sufficient — a real miss caught only after a live 401)**: (1) `User.Invite.All` (Delegated) added to the Entra app registration's API permissions and admin-consented via "Grant admin consent for Default Directory." (2) **Also required, initially missed**: `CONNECT_SCOPES` in `app/api/connectors/onedrive/route.ts` had to be updated to actually *request* `User.Invite.All` in the OAuth `scope` parameter — admin-consenting a permission on the app registration only makes it available to request, it does not automatically get included in issued tokens; the app's own OAuth flow has to list it explicitly (this file's own pre-existing doc comment already said as much for the original three scopes, and was correct — the gap was in not applying that same lesson to the new scope). Diagnosed via a live 401 "Insufficient privileges" on the new `preCreateGuestInvitation` call after reconnecting — the reconnect consent screen visibly listed only the original 4 permissions with no "Invite guest users to the organization" line, which was the correct signal but was initially misread as a UI-summarization quirk rather than "the scope isn't being requested." Corrected 2026-08-14; reconnecting after the `CONNECT_SCOPES` fix shows the new permission in the consent screen as expected. As with any `CONNECT_SCOPES` change, already-connected connectors must disconnect/reconnect (fresh OAuth consent) to pick up the new scope — existing/refreshed tokens don't retroactively gain it.
>     - **No regression risk to Google Drive** (explicitly verified per user question): change is fully contained inside `onedrive-permission-adapter.ts`'s `grantItemPermission` — a OneDrive-only file implementing one branch of the shared `IConnectorPermissionAdapter` interface. `google-drive-permission-adapter.ts` is a separate, untouched implementation; the interface contract (`grantFilePermission`'s signature/return type) is unchanged, so nothing calls into or is affected by the new OneDrive-only pre-create step.
>     - **Known limitation, not yet hit, documented proactively**: `preCreateGuestInvitation`'s `POST /invitations` call succeeds only if the connecting user's tenant allows it. Checked live 2026-08-14 (Entra ID admin center → External Identities → External collaboration settings) on the `firmaone365` tenant: **"Guest invite restrictions" = "Anyone in the organization can invite guest users including guests and non-admins (most inclusive)"** and **"Collaboration restrictions" = "Allow invitations to be sent to any domain (most inclusive)"** — both confirmed as this tenant's unmodified, out-of-the-box defaults, not a deliberate admin choice made for this integration. Under this default, the connecting account does NOT need to be a tenant admin for guest pre-creation to work (the admin-consent step earlier was only needed once, to approve the `User.Invite.All` scope on the app registration itself — a one-time, per-customer-tenant action, not a standing requirement of the connecting user's own role). However, a customer tenant whose IT admin has tightened "Guest invite restrictions" to "Only users assigned to specific admin roles" (not the default) would cause `preCreateGuestInvitation` to fail for a non-admin connecting user, even with `User.Invite.All` correctly granted and requested — a new failure mode distinct from the `sharingFailed`/`external_sharing_blocked` case item 15 already handles. Not handled in code yet; `preCreateGuestInvitation`'s current failure path just logs a warning and lets `driveItem: invite` proceed (and fail on its own with the existing `sharingFailed` messaging) — if this is ever hit in practice, it would need its own distinct, more accurate error message rather than being folded into the generic "sharing policy blocked" copy.
>     - `npx tsc --noEmit` clean. Live-tested 2026-08-14 after the `CONNECT_SCOPES` fix and a fresh reconnect — pending final confirmation of a successful external grant.
> 18. **DONE (2026-08-15) — Fixed the blank "enter your email" sign-in prompt for a genuinely new external guest opening a OneDrive/SharePoint document; then found and reverted a regression introduced while fixing its follow-on timeout.** Live test: a brand-new external guest opening a bare `driveItem.webUrl` for the first time landed on a generic, identity-blind `login.microsoftonline.com` sign-in page requiring manual email entry — jarring, since the app already knows exactly who's being invited.
>     - **Fix**: `onedrive-permission-adapter.ts`'s `preCreateGuestInvitation` (item 17) now also captures and returns Graph's `inviteRedeemUrl` from the `POST /invitations` response — a ticket-bound redemption link tied to that specific invitation record, which resolves identity server-side rather than prompting for it. New optional `IConnectorPermissionAdapter.preInviteGuest()` method (OneDrive-only; Google's adapter doesn't implement it) exposes this to `regrant/route.ts`, which now resolves the document's `webViewLink` first (used as `inviteRedirectUrl`, so redemption lands on the document, not a generic page), calls `preInviteGuest`, and prefers the returned `inviteRedeemUrl` over the bare `webViewLink` as the `documentUrl` returned to the frontend when available. Confirmed live: the blank email-entry screen is gone — a fresh guest now lands directly on the OTP/account-security/org-consent sequence (item 15/17's already-documented, Microsoft-owned friction, unaffected by this fix and not further reducible — see the "such a poor user experience" discussion in-session, concluded as an inherent one-time cost per new external identity, not a further-fixable bug).
>     - **Regression #1 (parallelization), found and reverted same day**: to keep this new step from blowing the client's 15s request timeout (it pushed real requests to 16.6s), `preInviteGuest` and `grantFilePermission` were briefly changed to run in `Promise.all` instead of sequentially. This broke access entirely: opening the resulting `inviteRedeemUrl` completed sign-in successfully but then hit SharePoint's own "You need access" page. Initial hypothesis — that the two concurrent Graph calls raced and resolved to two different *guest objects* — was investigated but turned out to be WRONG (see Regression #2 below, which is the real cause and persisted even after reverting to sequential). Reverted to strictly sequential anyway (`preInviteGuest` fully completes, then `grantFilePermission` runs) since latency wasn't the actual constraint once Regression #2 was found; the timeout problem is addressed by raising `use-secure-open-document.ts`'s client-side timeout 15s → 45s instead.
>     - **Regression #2 (the actual root cause), found via added identity-comparison logging after Regression #1's fix didn't resolve the symptom**: `grantItemPermission` (the function behind `grantFilePermission`) had ALSO been calling `preCreateGuestInvitation()` internally the whole time — added in item 17 as a defensive "safety net" in case a future caller skipped the explicit pre-invite step, and assumed harmless/idempotent since the underlying guest object *is* correctly reused (confirmed via logs: same `invitedUserId` both times). That assumption was wrong: **each `POST /invitations` call mints a NEW, DIFFERENT `inviteRedeemUrl` ticket even when it returns the same underlying guest object id.** Sequence, confirmed via live logs with a real external Gmail recipient: (1) `regrant/route.ts` calls `preInviteGuest` explicitly → ticket A (`user=c582a25c...`), captured and returned to the frontend as `documentUrl`. (2) `grantFilePermission` runs next, and INSIDE `grantItemPermission`, the redundant internal `preCreateGuestInvitation()` call fires again for the same email → same guest object, but a brand-new ticket B (`user=33f48c15...`) is minted and silently discarded (never captured/returned). (3) The actual `driveItem: invite` grant succeeds and correctly lands on the right guest object (`grantedToUserId` matched `invitedUserId` from both calls). (4) But issuing ticket B appears to invalidate ticket A — the user, already mid-redemption on ticket A (the one actually returned in `documentUrl` and opened), completes sign-in successfully but then hits "You need access," because the ticket they're redeeming no longer resolves cleanly even though the underlying permission is fine. **Fix**: removed the redundant internal `preCreateGuestInvitation()` call from `grantItemPermission` entirely — `regrant/route.ts` (the only real caller) always pre-invites explicitly before calling `grantFilePermission`, so the "safety net" was never needed for the current call graph, and generating a second ticket was actively harmful, not just wasted latency. A comment marks this for any future caller that might need the app-only new-guest workaround without also wanting `inviteRedeemUrl` — call `preCreateGuestInvitation` directly instead of relying on `grantItemPermission` to do it implicitly.
>     - **For internal recipients**: `preInviteGuest` still fires unconditionally (the route doesn't distinguish internal vs. external ahead of time) but fails harmlessly — Graph's confirmed (if undocumented) response for an email that's already a tenant Member is "The invited user already exists in the directory as objectID: {id}..." — matched by the same `already exists` substring check already used for the existing-guest case, so it's silenced (not logged as a warning) and falls back to `webViewLink`, identical to pre-this-item behavior. Confirmed live: internal-recipient regrants are unaffected by any of item 18's changes.
>     - Also improved `SecureAccessModal` per user request: broken/failed thumbnail images (`onError`) now fall back to the mimeType-based `DocumentIcon` instead of showing a broken-image glyph; the "Please wait…" button in the `loading` state now shows an animated 3-dot bounce indicator instead of static text.
>     - `npx tsc --noEmit` clean. **CONFIRMED WORKING end-to-end, 2026-08-15**, real external Gmail recipient (`mail.deepakshettigar@gmail.com`), returning guest (not first-time): OTP-only verification (no repeat org-consent screen, since already granted from an earlier attempt), landed directly on the document, opened successfully and editable in Word Online. Note on the investigation tail: several retests after the redundant-invite-call fix still failed with "You need access" despite server logs showing fully consistent, correct identities (same `invitedUserId`/`grantedToUserId`/ticket across calls) — extensive research into ticket expiry/reuse and SharePoint permission-propagation lag (see prior bullet's Q&A citations) found no documented mechanism explaining it. The failures stopped as soon as testing moved to a **fresh incognito/InPrivate session** — strongly suggesting those specific failures were stale browser/session state accumulated from repeatedly testing the same guest in one long-lived InPrivate window (a half-completed earlier redemption, a cached auth cookie, etc.), not a server-side or Graph-side bug. The server-side fix (single pre-invite call, correct identity chain, `inviteRedeemUrl` usage) was correct throughout that investigation; only the test methodology was the remaining confound. Recommend testing this flow in a fresh browser session going forward to avoid the same false signal.
>
> **2026-08-05 — Full connector-abstraction audit and fix pass.** Prompted by a real bug (Client/Engagement folders not provisioning for a live OneDrive connector — traced to `shareConnectorWithClient`/`createClient` in `lib/actions/client.ts` calling `googleDriveConnector` directly instead of the registry), a systematic audit found the *entire* document-lifecycle surface beyond Phase 0's original 4 routes was still hardcoded to Google Drive: upload, Engagement>Files browsing/create-folder/duplicate/copy/move/move-tree/cross-engagement-copy-move/rename (`linked-files/route.ts`, ~1000 lines, 10+ call sites), single + bulk download, sharing enforcement + regrant fallback lookup, and search indexing/text-extraction. All of these were routed through the existing `lib/connectors/registry.ts` adapters (`getStorageAdapter`/`getPermissionAdapter`/`getContentAdapter`) instead of calling `googleDriveConnector`/`GoogleDriveConnector` directly, following the same pattern Phase 0 established. New files: `lib/connectors/adapters/onedrive-list-files.ts` (folder-tree/role-based file listing, mirrors Google's `listFiles`), `lib/connectors/adapters/onedrive-file-ops.ts` (move/rename/copy/recursive-copy/ensure-folder-path against Graph). `IConnectorPermissionAdapter.getEngagementFolderIds` — previously a stub returning nulls for OneDrive — now reads back what `pockett-structure.service`'s (already provider-agnostic) `ensureAppFolderStructure` wrote. This pass traded some regression risk for cleaner abstraction (per explicit user direction) — Google's code paths were largely left untouched structurally (same methods, same call shapes) but now sit behind a provider branch instead of being the only path; **both providers need live end-to-end testing** before this is considered fully verified, per that same explicit trade-off.
>
> **Status refresh — 2026-07-27.** Phase 0 and Phase 1a-signin are merged to `dev` (not just a feature branch — the earlier feature branch was squash-merged via PR #82, then further fixes landed directly on `dev`). This refresh reconciles the plan with what's actually shipped; see the "Status as of 2026-07-27" block right after the phase table for the full delta.

## Overview of phases

Reordered so the three phases flagged **Low** regression risk (1b, 2, 5) run first, right after Phase 0 — they're additive and don't touch Google Drive code paths. Phase 3 (Low-**Medium**) stays gated behind Phase 2, since site selection needs the Graph adapter to exist first. A new Phase 1a is inserted ahead of everything else in the OneDrive track: the external Azure/Microsoft Entra portal configuration that Phase 1b's OAuth code depends on to even run.

| Order | Phase | Goal | Status | Regression Risk | Impact if it breaks |
|---|---|---|---|---|---|
| 1 | **0** | Abstract document-lifecycle Drive coupling (preview, support attachments, sharing regrant) behind existing adapter interfaces — no OneDrive code yet | **✅ Done, merged to `dev`** — support attachments further redesigned post-merge, see status block below | **Medium** — refactors live Google Drive code paths; risk is behavior drift, not new failure modes (see per-step risk below) | Google Drive users (100% of current base) hit broken preview, broken attachment upload/download, or broken sharing — this is the only phase that touches paths already in production use |
| 2 | **1a** | External Azure/Microsoft Entra app registration & OAuth config — manual portal work, not code | **✅ Done (2026-07-22)** — app registered, secret + local Supabase Azure provider wired; Phase 1b code not yet written so end-to-end OAuth is untested | **N/A** — no app code touched; risk is entirely in getting the Azure config right | Blocks all OneDrive OAuth connection attempts until fixed; zero impact on existing Google Drive users (isolated new infrastructure) |
| 2b | **1a-signin** | "Sign in with Microsoft" — smallest possible slice that exercises Phase 1a's Azure app registration + Supabase Azure provider end-to-end via a real browser OAuth round-trip, with zero Graph/connector code. Serves as the foundation smoke test before investing in Phase 1b/2. | **✅ Done, merged to `dev` — but hidden behind `MICROSOFT_SIGNIN_ENABLED` (defaults `false`)**, see status block below | **Low** — one new button + one new function mirroring `signInWithGoogle`; touches the shared sign-in page but adds a new code path rather than modifying the existing Google one | Currently zero impact — feature is flagged off in production. When enabled: users clicking "Continue with Microsoft" see an error if misconfigured; zero impact on Google/OTP sign-in, a separate code path |
| 3 | **1b** | Microsoft Graph OAuth code (connect flow, token storage/refresh) — consumes Phase 1a's credentials | **Not started — next up** | **Low** — additive only; new routes, new env vars, no existing code path modified | None to existing users; failure only blocks new OneDrive connections from working |
| 3 (parallel) | **2** | Real adapter implementations — fill `onedrive-connector.ts`/`onedrive-adapter.ts` stubs, implement `IConnectorPermissionAdapter`/`IConnectorMigrationAdapter`/`IConnectorContentAdapter` (from Phase 0) against Microsoft Graph | Not started | **Low** — new implementation behind existing interfaces; Google Drive adapters untouched | None to existing (Google) users; OneDrive users see broken folder/file/permission operations until fixed |
| 4 | **5** | UI — enable OneDrive tab, connector icon, connect flow, file picker | Not started | **Low** — new components + a config flip (`enabled: false → true`); no shared UI components modified | OneDrive users only; worst case is a broken connect flow, not a regression for existing Google Drive users |
| 5 | **3** | SharePoint site selection (site picker, `workspaceRootSharedStorageId` wiring — schema already generic) | Not started | **Low-Medium** — reuses existing generic schema fields, but first real write path to `workspaceRootSharedStorageId` for a non-Google provider; could expose latent assumptions in that field's Google-only history | OneDrive/SharePoint users only; a misconfigured site link could point a workspace root at the wrong SharePoint site (data-exposure risk, contained to opted-in OneDrive connectors) |
| 6 | **6** | Testing — adapter/permission/migration/content test suites mirroring existing Google ones | Not started | **None** — test-only, no production code changes | None; catching gaps here prevents risk in Phases 1b-3 from reaching production undetected |

**Dependency notes:**
- Phase 1a has zero dependency on anything — it's Azure Portal work only, can start immediately.
- Phase 1a-signin depends only on Phase 1a's completed Azure config (done) and the `[auth.external.azure]` Supabase block (done). It has no dependency on Phase 1b/2/5 — deliberately the next thing to build so Phase 1a's foundation is verified working before further investment.
- Phase 1b depends on Phase 1a's credentials (`MICROSOFT_CLIENT_ID`/`MICROSOFT_CLIENT_SECRET`/`MICROSOFT_TENANT_ID`) to actually run/test, though the route code itself can be scaffolded in parallel.
- Phase 2 depends on Phase 0's interfaces (done) but not on live Microsoft credentials until integration testing — can be scaffolded in parallel with 1a/1b.
- Phase 5 (UI) trails Phase 1b — the "Connect" button needs a working OAuth flow to wire into, though icons/tab scaffolding can start earlier.
- Phase 3 stays gated behind Phase 2 (needs the adapter to persist a real site selection) — not reordered forward despite being close to Low risk.
- Phase 6 stays last as the cross-cutting safety net regardless of implementation order.

Each phase gets its own detailed plan (like Phase 0's) once the prior phase lands, since later phases depend on interface shapes Phase 0 finalizes and on real Graph API behavior not yet validated against this codebase.

---

## Status as of 2026-07-27 (reconciliation — read before starting Phase 1b)

Everything below happened after Phase 1a-signin's original "Status: ✅ Done" note further down this doc, and isn't reflected in the phase-by-phase sections below. Read this block first; treat the detailed sections after it as historical record of *what was planned*, not current truth for anything listed here.

1. **Support ticket attachments were redesigned, superseding Phase 0 Step 2 as written.** Not Drive-backed anymore at all. During manual QA, a real bug was found: client-less support tickets (the common case — firm-level bug reports have no client/engagement selection) had no connector to resolve, so attachment upload always failed. Rather than patch connector-resolution, attachments were moved entirely off Drive to DB-blob storage — base64 data URLs stored inline in `CustomerRequest.attachments` (JSONB), mirroring the existing `Brand.logoData` pattern. New file `frontend/lib/support-ticket-auth.ts`. The `driveFileId`-keyed routes (`attachments/[driveFileId]/{delete,download}`, `support/attachments/prepare-upload`) were deleted; attachments are now keyed by a UUID `attachmentId`. This is scoped **only** to support tickets — engagement documents/client deliverables still use Google Drive via the Phase 0 adapters exactly as planned.
2. **A real cross-tenant authorization bug was found and fixed in the same area.** All 5 support-ticket mutation routes (`upload-attachment`, `attachments` PATCH/DELETE, `status` PATCH, `comments` POST) checked only "is this a valid logged-in user" — never that the caller owned the ticket or administered its firm. Any authenticated user of any firm could read/modify/delete another firm's ticket data by ticket number (displayed in-app, not a secret). Fixed via `canAccessSupportTicket()` (creator OR firm_admin of the ticket's firm) applied to all 5 routes. Also fixed in the same pass: upload size cap now enforced via `Content-Length` before buffering (was buffering unbounded bodies before rejecting), and client-side 50MB cap corrected to match the server's actual 8MB limit.
3. **"Sign in with Microsoft" is built but hidden behind `MICROSOFT_SIGNIN_ENABLED`** (env var, defaults `false`, not `NEXT_PUBLIC_` — server-gated in `signin/page.tsx` and `signup/page.tsx`, threaded down as a prop; fails closed if omitted). Reason: **publisher verification is structurally blocked**, not just "not done yet" — Microsoft's own rules state apps registered under a personal Microsoft account (this one was) can never be publisher-verified, and there's no registered business entity to complete Partner Center's business-verification step even if the app were re-registered. Until both are resolved (new app registration under a real work-account tenant + an actual registered business), Microsoft sign-in ships flagged off to avoid showing real users an "unverified publisher" warning at first sign-in. Microsoft OAuth is also now wired into **signup**, not just signin (`AuthService.signInWithMicrosoft` in `auth-service.ts`, `getApplicableOAuthProviders`/`isMicrosoftEmail`/`isPotentiallyMicrosoftEntra` in `email-utils.ts` — mirrors the existing Google email-heuristic gating on `/signup`).
4. **Publisher domain partially progressed**: `www.firma.bz` is now the app's verified Publisher domain (via `frontend/public/.well-known/microsoft-identity-association.json`) — this was a real, completable step and is done. The MPN/Partner Center verification badge itself remains blocked per point 3.
5. **An unrelated production bug was found and fixed while testing Microsoft sign-in**: `signInWithGoogle`/`signInWithMicrosoft` in `auth-context.tsx` (the sign-**in** path, not sign-up) used a hardcoded `config.appUrl` (`www.firma.bz`) as the OAuth `redirectTo` origin regardless of the user's actual host. Since Supabase's PKCE `code_verifier` cookie is host-scoped with no `domain` attribute, any user signing in from a client subdomain (`app.firma.bz`, `datasentry.firma.bz`, `keithmeyer.firma.bz`, now also `scarbluu.firma.bz`) got `AuthPKCECodeVerifierMissingError` on first attempt — cookie set on the subdomain, callback bounced to `www.firma.bz`, cookie not found. Fixed to always use `getOAuthRedirectOrigin()` (current origin). Confirmed live in production before the fix (user reproduced it on `app.firma.bz`). `docs/mvp/hld-custom-dns.md` was also corrected — it incorrectly claimed no app-code changes were needed per new client subdomain; both this bug and the `CLIENT_SUBDOMAINS` array in `proxy.ts` are real required code touchpoints.
6. **Workflow change**: the feature branch (`feature/connector-abstraction-phase0-1a`) was squash-merged to `dev` via PR #82 and deleted. Going forward, Microsoft/OneDrive work continues **directly on `dev`**, gated by feature flags (`MICROSOFT_SIGNIN_ENABLED` today; Phase 1b/2/5 will need their own flag, e.g. `ONEDRIVE_CONNECTOR_ENABLED`) rather than a long-lived branch — avoids branch/dev drift, allows unrelated fixes (like point 5) to ship to production independently of Microsoft-integration progress.
7. **Manual QA status for Phase 0's original Step 3/4 (preview, sharing regrant)**: a code-level review (not full manual QA) confirmed 4 of 5 untested preview branches (`.gdoc` stub, already-PDF, unsupported-type, forbidden-file) are verbatim moves from pre-refactor code and covered by new unit tests (`google-drive-connector.getPreviewableContent.test.ts`) — real risk is low. The full live sharing-regrant flow (share-original, share-PDF-only, revoke, copy-restriction toggle) remains **not manually tested end-to-end** — this is still the single highest-risk open item from Phase 0, per the plan's own original risk assessment (Step 4 = "highest regression risk of the four steps").

**Net effect on what's actually pending going into Phase 1b:** the plan below is otherwise accurate — Phase 1b (Graph OAuth connector routes) has not been started, and everything from Phase 2 onward is unstarted. The main things a fresh read of the *unmodified* sections below would get wrong are: (a) assuming support attachments still touch Drive at all (they don't), (b) assuming Microsoft sign-in is live for real users (it's flagged off), and (c) missing that a feature-branch workflow is no longer how this work ships.

### Phase 1a — External Azure/Microsoft Entra OAuth Configuration (manual, portal-based)

**Type:** Manual work in the Azure Portal / Microsoft Entra admin center (entra.microsoft.com) — not code. This is the user's own action; Claude cannot execute it. Produces the credentials Phase 1b's code needs.

**Regression risk:** N/A (no existing code touched). **Impact if misconfigured:** blocks all OneDrive OAuth connection attempts; zero impact on existing Google Drive users since this is entirely new, isolated infrastructure.

**Steps:**

1. **Prerequisite check** — no paid Azure subscription required. Microsoft Entra ID Free is bundled with any Azure or Microsoft 365 account and is sufficient for app registration + OAuth. Needs the **Application Developer** role in some tenant (a free default directory works).

   **Gotcha — personal Microsoft accounts have no directory by default.** A plain personal account (outlook.com/hotmail/live, never used to sign up for Azure or M365) has nothing to register an app against yet. Navigating to "Manage Microsoft Entra ID" from a personal-account settings surface (e.g. account.microsoft.com) errors out because there's no tenant behind it — Azure doesn't auto-provision a directory from that path.

   **Fix — create a free Azure account to auto-provision a directory:**
   - Go to [azure.com/free](https://azure.com/free) and sign up using the personal Microsoft account, or a different Microsoft account if this one keeps hitting issues (a new outlook.com account works fine — no existing history required).
   - No paid subscription needed to use Entra ID Free; Azure may ask for a card for identity verification only.
   - This provisions an Azure tenant with a default Entra ID directory, and makes the signing-in account its Global Administrator.
   - Once this completes, entra.microsoft.com → App registrations → New registration will work, since a directory now exists to register into.
   - If the personal account continues to fail (e.g. flagged, region-restricted, or stuck in a bad state), repeat this step fresh with a different Microsoft account — the app registration itself isn't tied to any specific personal account, only to whichever directory/tenant ends up hosting it.

   **Cosmetic error to expect and ignore — "Interaction required" / AADSTS16000 on the Entra Overview page.** After clicking "Manage Microsoft Entra ID" → View, the tenant Overview page may show a popup: `AADSTS16000: User account '{EUII Hidden}' from identity provider 'live.com' does not exist in tenant 'Microsoft Services' and cannot access the application '...' (ADIbizaUX) in that tenant.` This is the portal's "My feed" widget silently trying to reach an unrelated Microsoft-internal tenant ("Microsoft Services") for news/feed content — it has nothing to do with your actual tenant, which is already working (Tenant ID, Users, License = Microsoft Entra ID Free all render fine underneath the dialog). Click **Ignore** and proceed to Manage → App registrations → New registration.

2. **Register the app** — entra.microsoft.com → Entra ID → App registrations → New registration.
   - Name it (e.g. "Pockett — OneDrive/SharePoint").
   - **Supported account types: select "Accounts in any organizational directory (Any Microsoft Entra ID tenant - Multitenant) and personal Microsoft accounts"** — the only option that lets both work/school (SharePoint-capable) and personal OneDrive accounts sign in.
   - Register. Copy the **Application (client) ID** and **Directory (tenant) ID** from the Overview page.

3. **Configure the redirect URI** — Authentication → Add a platform → **Web** (not SPA, not Mobile/Desktop — Web is correct for a Next.js server-side confidential-client auth-code flow, matching the existing Google OAuth pattern at `app/api/connectors/google-drive/callback/route.ts`).
   - Add `https://<your-domain>/api/connectors/onedrive/callback` (and a `localhost` equivalent for local dev), matching Phase 1b's planned route.

4. **Generate a client secret** — Certificates & secrets → Client secrets → New client secret.
   - Expiry ≤12 months (Microsoft's max is 24; shorter is recommended).
   - **Copy the secret's Value immediately** — shown only once. (Not the "Secret ID," which is just a management identifier.)
   - This becomes `MICROSOFT_CLIENT_SECRET`.

5. **Scope decision — "Sign in with Microsoft" is now in scope alongside the storage connector.** This changes what a single app registration must support and is worth getting right before configuring API permissions.

   **The problem:** requesting broad scopes (`Files.ReadWrite.All`, `Sites.ReadWrite.All`) on every Microsoft OAuth redirect — including a login button — would show users a scary, over-permissioned consent screen just to sign in, and risks tripping tenant admin-consent blocks for users who only want to log in, not connect storage.

   **The fix — one app registration, two separate authorize requests with different scope sets.** OAuth scopes are requested per authorize-URL call, not fixed to the app registration; "API permissions" in Entra ID is the *superset* of scopes the app is allowed to ever request, not what every login must ask for. So:
   - **Sign-in flow** (`app/api/auth/microsoft/{route,callback}.ts` or equivalent, new in this phase) requests only `openid profile email User.Read` — an identity-only consent screen, same weight as "Continue with Google" today.
   - **Connect OneDrive/SharePoint flow** (Phase 1b's existing planned route) requests `Files.ReadWrite.All Sites.ReadWrite.All offline_access` — shown only when a user deliberately clicks "Connect OneDrive" inside the already-authenticated app, never at login.
   - Both flows share the same `MICROSOFT_CLIENT_ID`/`MICROSOFT_CLIENT_SECRET`; only the `scope` query param differs between the two authorize URLs.
   - This mirrors the existing Google pattern in this codebase, which uses **separate client IDs** (`GOOGLE_CLIENT_ID` for Supabase sign-in vs `GOOGLE_DRIVE_CLIENT_ID` for the Drive connector) to achieve the same scope separation — Microsoft's approach differs (one app, two scope sets) because Supabase's Azure provider needs its own client ID/secret pair configured in Supabase Auth settings regardless of how many Entra app registrations exist, so reuse is simpler here than mirroring Google's two-client-ID split exactly.

   **New work this adds to the plan (not previously scoped):**
   - Supabase Auth: enable and configure `[auth.external.azure]` in `frontend/supabase/config.toml` (currently absent — only `[auth.external.google]` exists) with `MICROSOFT_CLIENT_ID`/`MICROSOFT_CLIENT_SECRET` and scope `openid profile email User.Read`.
   - Sign-in UI: add a "Continue with Microsoft" button to `frontend/app/(app)/signin/signin-view.tsx`, alongside the existing Google button, calling `supabase.auth.signInWithOAuth({ provider: 'azure', ... })`.
   - **Confirmed via GoTrue source (`supabase/auth` `internal/api/provider/azure.go`):** Supabase's `azure` provider scope is controlled entirely client-side per `signInWithOAuth` call — it defaults to `openid` only if `options.scopes` is omitted, and never reads/injects the Azure app registration's configured Graph API permissions. Safe to share one app registration with the connector. **The sign-in button's code must explicitly pass `options: { scopes: 'email profile openid' }`** (email is required — Supabase Auth needs a valid email and does not request it by default) and must NOT include `Files.ReadWrite.All`/`Sites.ReadWrite.All` in that call. `frontend/supabase/config.toml`'s `[auth.external.azure]` block has no scope setting of its own — scope is entirely a client-call concern, not a config-file concern.
   - This sign-in work is a distinct code change from Phase 1b's OneDrive connector OAuth code; track it as its own step/PR rather than bundling into Phase 1b, since it touches the shared sign-in page rather than being purely additive/isolated like the rest of Phase 1b.

6. **Configure API permissions (delegated Microsoft Graph scopes)** — API permissions → Add a permission → Microsoft Graph → Delegated permissions. This is the full superset available to the app; individual authorize requests (per step 5) will only ask for the subset relevant to that flow:
   - `openid`, `profile`, `email` — standard OIDC
   - `User.Read` — basic profile
   - `offline_access` — required to receive a refresh token
   - `Files.ReadWrite.All` — OneDrive file access (broader than `Files.ReadWrite`, matches Google Drive's `drive.file`/`drive.appdata` breadth)
   - `Sites.ReadWrite.All` — SharePoint site access
   - None of these delegated permissions require tenant-admin consent per Microsoft's permission table, but many customer tenants enforce admin consent anyway via risk-based step-up consent policies — build for that (see step 8).
   - **Decision point:** `Sites.ReadWrite.All` is broad (flagged prominently on the consent screen) versus `Sites.Selected`, which is least-privilege but requires an additional per-site grant call (`POST /sites/{siteId}/permissions`) after consent, meaning Phase 3 would need to build that grant step too. Recommend starting with `Sites.ReadWrite.All` for a simpler Phase 3; revisit `Sites.Selected` later if customers push back on the broad scope.

7. **Note the OAuth endpoints for Phase 1b's code:**
   - Authorize: `https://login.microsoftonline.com/common/oauth2/v2.0/authorize`
   - Token: `https://login.microsoftonline.com/common/oauth2/v2.0/token`
   - Use the `common` tenant segment (not `organizations` or `consumers`) for sign-in, since the app is registered for both personal and work/school accounts.

8. **Plan for the admin-consent flow (multi-tenant SaaS specifics)** — each customer firm has its own Microsoft 365 tenant, separate from Pockett's. For firm-level (not just individual-user) onboarding, Phase 1b's code should support the explicit admin-consent endpoint:
   `https://login.microsoftonline.com/{tenant}/v2.0/adminconsent?client_id=...&scope=...&redirect_uri=...&state=...`
   using `organizations` or the specific customer tenant ID (never `common` — personal accounts can't grant admin consent). This mirrors a "Connect your organization" step a firm's IT admin clicks through once.

9. **Publisher verification (recommended, not strictly required to function)** — unverified multi-tenant apps show an "unverified publisher" warning on the consent screen, and tenants with risk-based step-up consent may block self-service consent entirely for unverified apps requesting broad scopes like `Sites.ReadWrite.All`. Verification is free: enroll in the Microsoft AI Cloud Partner Program (a Partner Global Account), register the app under a work/school account, use a non-`onmicrosoft.com` publisher domain. Recommended before onboarding real customer firms, since it directly affects whether firm admins trust/complete the consent flow.

10. **Capture and store credentials as env vars** (consumed by Phase 1b):
    - `MICROSOFT_CLIENT_ID` — Application (client) ID from step 2
    - `MICROSOFT_CLIENT_SECRET` — secret Value from step 4
    - `MICROSOFT_TENANT_ID` — Directory (tenant) ID from step 2 (used for admin-consent flows; sign-in itself uses `common`)
    - Add to `.env`/`env.example` alongside the existing `GOOGLE_DRIVE_CLIENT_ID`/`GOOGLE_DRIVE_CLIENT_SECRET` pattern in `lib/config.ts`.

11. **Understand refresh token behavior for later phases** — v2.0 refresh tokens have a 90-day sliding lifetime (each use rotates in a new one, resetting the clock for active connections). A customer firm's connection left unused for ~90 days needs re-consent. Phase 1b/2's token-refresh code should handle `invalid_grant`/`interaction_required` errors by marking the connector `EXPIRED` and prompting reconnection — the same pattern already used for Google Drive in `google-drive-connector.ts`'s `refreshAccessToken`.

**Costs:** Free. App registration and Graph API calls incur no Azure charges under Entra ID Free. Costs only apply if other Azure resources are separately provisioned (not needed here) or Entra ID P1/P2 governance features are added later (not required for OAuth).

**Files/docs to update once done:**
- `frontend/env.example` / `env.production.example` — add `MICROSOFT_CLIENT_ID`, `MICROSOFT_CLIENT_SECRET`, `MICROSOFT_TENANT_ID` placeholders
- This plan file — mark Phase 1a done, record the actual client ID/tenant ID (not the secret) for reference, note which scope choice (`Sites.ReadWrite.All` vs `Sites.Selected`) was made

**Verification:**
- App registration's Overview page shows the correct Application (client) ID and Directory (tenant) ID
- Redirect URI is registered under a **Web** platform, exact match to the planned callback route
- All permissions listed under API permissions with expected consent status
- Manually test the authorize URL in a browser (placeholder redirect) to confirm the Microsoft consent screen renders and lists the requested scopes correctly, for both a personal Microsoft account and a work/school account

---

## Phase 1a — Actual results (done 2026-07-22)

**App registration:**
- Name: **Firma Connect**
- Application (client) ID: `58e93077-c3c2-426d-ac6d-96be7b877c61`
- Directory (tenant) ID: `6e837b68-c5f0-4bb1-a1ff-5a739da980d0` ("Default Directory")
- Supported account types: **Any Entra ID Tenant + Personal Microsoft accounts** (multitenant + personal), as planned
- Client secret: description "Firma Connect - OneDrive", **expires 2028-07-21 (24 months — longer than the plan's ≤12-month recommendation; user's explicit choice)**. Rotate before this date or Phase 1b's OAuth flows break with `invalid_client`.
- Redirect URIs registered (Web platform):
  - `http://localhost:3000/api/connectors/onedrive/callback` (local dev)
  - `https://www.firma.bz/api/connectors/onedrive/callback` (production)
  - `http://localhost:54321/auth/v1/callback` (local Supabase Auth callback — see gotcha below)
  - *(Still needed before Phase 1b ships: a production Supabase Auth callback URI, e.g. `https://<project>.supabase.co/auth/v1/callback`, once the "Sign in with Microsoft" button is built.)*
- API permissions granted (all Delegated, Microsoft Graph): `openid`, `profile`, `email`, `User.Read`, `offline_access`, `Files.ReadWrite.All`, `Sites.ReadWrite.All`. Scope choice made: **`Sites.ReadWrite.All`** (not `Sites.Selected`), per the plan's simpler-Phase-3 recommendation.
- Credentials stored in `frontend/.env` (git-ignored) as `MICROSOFT_CLIENT_ID` / `MICROSOFT_CLIENT_SECRET` / `MICROSOFT_TENANT_ID`; placeholders added to `env.example` and `env.production.example`.

**Scope change from original plan — "Sign in with Microsoft" added:** partway through this phase, the user decided Microsoft sign-in should ride on the same app registration as the OneDrive/SharePoint connector (see the new step 5 above, "Scope decision"). This was **not** in the original Phase 1a/1b scope and adds real Phase 1b-adjacent work not yet built:
- `frontend/supabase/config.toml` now has an `[auth.external.azure]` block (added this phase, previously absent — only `[auth.external.google]` existed).
- Still needed in code (Phase 1b or a dedicated sub-step): the actual "Continue with Microsoft" button in `frontend/app/(app)/signin/signin-view.tsx`, calling `supabase.auth.signInWithOAuth({ provider: 'azure', options: { scopes: 'email profile openid' } })`. **Must not** pass the Graph file/site scopes in this call — confirmed via GoTrue source that Supabase never mixes in the Azure app registration's configured Graph permissions automatically, so the sign-in consent screen stays narrow as long as the client call itself stays narrow.

**Gotchas encountered and resolved this session (documented here so they aren't rediscovered from scratch):**

1. **Personal Microsoft accounts have no directory by default.** Navigating "Manage Microsoft Entra ID" from a personal-account context can error because there's no tenant yet. Fixed by completing a *full* signup at [azure.com/free](https://azure.com/free) (not just clicking into an ambient/default tenant view) — this properly provisions an Azure tenant + default Entra ID directory and makes the account its Global Administrator. Card details are required for identity verification even on the free tier; no charge occurs unless you separately provision paid resources.
   - The Azure signup form's "Company name" field was required even under "For personal use" — worked around by entering a placeholder value (a real name string like "Self-employed"; literal "NA" was rejected by validation).

2. **Cosmetic "Interaction required" / `AADSTS16000` popup on the Entra Overview page** — text like `User account '...' from identity provider 'live.com' does not exist in tenant 'Microsoft Services' and cannot access the application '...' (ADIbizaUX)`. This is the portal's own "My feed" widget failing to reach an unrelated Microsoft-internal tenant; it's unrelated to your actual tenant/directory, which works fine underneath. **Click Ignore** and proceed — don't try to "fix" this by re-authenticating, which just re-triggers the same broken internal call. (If it appears blocking a real action like "New registration" itself rather than as a dismissable popup, that's a different, real problem — see gotcha 1's fix.)

3. **Azure Web redirect URIs reject bare loopback IPs.** Azure Portal's URI validation only accepts `https://` or the literal hostname `localhost` — `http://127.0.0.1:...` is rejected with "Must start with 'HTTPS' or 'http://localhost'". Since Supabase's local auth callback defaults to `127.0.0.1` in some configs, `frontend/supabase/config.toml`'s `[auth.external.azure].redirect_uri` was explicitly set to `http://localhost:54321/auth/v1/callback` (not `127.0.0.1`) to satisfy this — Google's redirect URI in the same file is untouched and still uses `127.0.0.1`, since Google's validation doesn't have this restriction. `NEXT_PUBLIC_SUPABASE_URL` (`http://127.0.0.1:54321`) also needed no change — it's a separate concern (app→Supabase API calls), not sent to Azure for validation.

4. **MFA "security defaults" prompt** — the tenant has Microsoft's baseline "security defaults" policy enabled (visible under Entra ID → Properties, green checkmark), which will prompt the signed-in user to register MFA (Microsoft Authenticator app recommended) at some point. This was skipped/deferred during this session after hitting an unspecified setup error; it does not block app registration, API permissions, or client secret creation, but should be revisited before this account is relied on long-term as the tenant's Global Administrator.

**Files/docs updated in this phase:**
- `frontend/env.example`, `frontend/env.production.example`, root `env.example`, root `env.production.example` — added `MICROSOFT_CLIENT_ID`/`MICROSOFT_CLIENT_SECRET`/`MICROSOFT_TENANT_ID` placeholders
- `frontend/.env` — actual local credentials added (git-ignored)
- `frontend/supabase/config.toml` — new `[auth.external.azure]` block added
- This plan file

### Phase 1a-signin — "Sign in with Microsoft" (foundation smoke test)

**Why this comes before Phase 1b/2:** Phase 1a's Azure app registration and Supabase `[auth.external.azure]` config are configured but never exercised by a real browser OAuth round-trip. This phase is the smallest possible slice that proves the foundation actually works — one button, one function, reusing 100% of Supabase's existing OAuth machinery — before investing in Phase 1b's Graph token-storage/refresh code or Phase 2's adapter implementations.

**Pattern to mirror:** `signInWithGoogle` in `frontend/lib/auth-context.tsx:90-115` and the "Continue with Google" button in `frontend/app/(app)/signin/signin-view.tsx:287-315`. Google's flow is gated behind email verification first (not a standalone one-click button) — decide whether Microsoft follows the same UX gate or is offered as an independent option; simplest is to mirror Google exactly for consistency.

**Code changes:**
1. **`frontend/lib/auth-context.tsx`** — add `signInWithMicrosoft(email?: string, next?: string): Promise<void>` alongside `signInWithGoogle`, following the identical `baseUrl`/`callbackUrl` localhost-vs-prod logic, but:
   ```typescript
   const { error } = await supabase.auth.signInWithOAuth({
     provider: 'azure',
     options: {
       redirectTo: callbackUrl,
       scopes: 'email profile openid',  // NOT Files.ReadWrite.All / Sites.ReadWrite.All — see Phase 1a step 5
       queryParams: email ? { login_hint: email } : undefined,
     },
   })
   ```
   Add `signInWithMicrosoft` to the `AuthContextType` interface and the context `value` object.
2. **`frontend/app/(app)/signin/use-sign-in-flow.ts`** — extend `handleEmailSubmit`'s `method` union from `'google' | 'otp'` to `'google' | 'microsoft' | 'otp'` (or add a parallel `handleMicrosoftSubmit`, matching however `handleEmailSubmit` is structured); add a `microsoftLoading` state mirroring `googleLoading`.
3. **`frontend/app/(app)/signin/signin-view.tsx`** — add a second outlined button below/beside "Continue with Google" using the same `OUTLINE_SECONDARY` class, a Microsoft logo SVG (4-color squares, not Google's colored "G"), and `{microsoftLoading ? 'Signing in…' : 'Continue with Microsoft'}`.
4. **`/auth/callback` route** — check whether it's Google-specific or provider-agnostic (Supabase's callback handler is typically generic across providers via `exchangeCodeForSession`); if provider-agnostic, no change needed here.
5. **Production Supabase redirect URI** — Phase 1a's Azure app registration still needs a production Supabase Auth callback URI added (e.g. `https://<project>.supabase.co/auth/v1/callback`) before this works outside local dev — noted as still-needed in Phase 1a's actual-results section above.

**Verification:**
- Local: click "Continue with Microsoft" on `/signin`, confirm the Microsoft consent screen shows only `email`/`profile`/`openid` scopes (not the broad Graph file/site scopes), complete sign-in with a personal Microsoft account, confirm redirect to `/auth/callback` and a working session — same end state as Google sign-in.
- Confirm a new Supabase `auth.users` row is created with `email` populated correctly (this was the specific risk flagged in Phase 1a step 5 — Supabase requires email and doesn't request it by default).
- Test with both a personal Microsoft account and a work/school account, matching the multitenant app registration.
- Confirm existing Google/OTP sign-in still works unmodified (regression check).

**Status: ✅ Done (2026-07-23, code + local manual test).**

- Implemented exactly as scoped: `signInWithMicrosoft` in `auth-context.tsx`, `microsoft` branch (explicit `else if`, avoiding the OTP-fallthrough risk flagged in the pre-implementation regression review) in `use-sign-in-flow.ts`, sibling button in `signin-view.tsx`. `tsc --noEmit` clean across the whole project — zero type errors, confirming the `AuthContextType`/`method` union changes have no ripple elsewhere.
- **Local manual test passed**, using `deepak@firmaone.com` (a Global Administrator on the "Default Directory" tenant from Phase 1a). Consent screen showed exactly two permission groups — "View your basic profile" and "Maintain access to data you have given it access to" (the `offline_access` refresh-token permission, explicitly described as granting no additional access) — confirming `Files.ReadWrite.All`/`Sites.ReadWrite.All` are correctly excluded from the sign-in scope request. Completed OAuth round-trip, redirected through `/auth/callback`, landed in a working authenticated session.
- **One retry needed**: first attempt hit `error=invalid_request&error_code=bad_oauth_state&error_description=OAuth+state+has+expired` — caused by pausing on the consent screen too long (reviewing/discussing it) before clicking Accept, not a bug. Supabase's OAuth `state` token has a short TTL; a prompt retry succeeded immediately. **Note for future testing**: don't linger on the Microsoft consent screen mid-flow.
- **Admin-consent checkbox confirmed tenant/role-gated, not shown to most real users**: researched and confirmed via Microsoft Learn — "Consent on behalf of your organization" only renders for users holding Privileged Role Administrator (or higher, e.g. Global Administrator) in a work/school tenant; it never appears for non-admin work/school users or personal Microsoft accounts (personal accounts hold no tenant role at all). It appeared in testing only because the test account is the tenant's Global Admin — expected, not a design issue.
- **Not yet tested**: work/school account sign-in (only personal-tenant-admin account tested so far), and the production Supabase redirect URI is still not registered in Azure (still open, noted above) — production sign-in won't work until that's added.
- **Not yet verified**: whether the new Supabase `auth.users` row has `email` populated correctly — recommend checking Supabase Studio (`http://127.0.0.1:54323`) before considering this fully closed.

**Regression risk:** Low — new function/button additions; the only shared-surface edit is `signin-view.tsx`'s JSX (adding a sibling button, not modifying Google's), and `use-sign-in-flow.ts`'s `method` type widening (existing `'google'`/`'otp'` branches untouched).

### Phase 1b/2/3/5 — Low-level design (added 2026-08-05, before implementation start)

**Read this before the per-phase sections below** — a code survey found two premises in the original high-level plan were wrong, and three design decisions were made explicitly with the user. Recorded here so implementation doesn't silently drift from what was actually decided.

**Corrections to the original plan, from a full code survey:**
1. **`getConnectorMeta(type)` in `registry.ts` (the "flip `enabled: false → true`" the plan describes) has zero UI consumers** — confirmed via repo-wide grep, nothing imports it. It's dead code.
2. **There is no tab-based, data-driven connector UI.** The real live UI is a single collapsible "Document Storage" section (`components/projects/firm-settings-form.tsx:862` → `<FirmDriveSection>`), not a tab list. Inside `firm-drive-section.tsx` (lines 695-708) the "OneDrive — Coming Soon" tile is **hardcoded JSX** (`disabled`, `opacity-50`, static "Coming Soon" badge) — this is the actual gate to replace, not `getConnectorMeta`. `client-drive-section.tsx` (client-level connectors, used by `client-settings-form.tsx:612`) has the same pattern and needs the same treatment.
3. **Stub files already exist and are already wired into `registry.ts`**: `lib/connectors/onedrive-connector.ts` (implements `IConnectorInstance`, all methods throw/no-op) and `lib/connectors/adapters/onedrive-adapter.ts` (implements `IConnectorStorageAdapter`, every method calls a `stub()` that throws). Both are already dispatched to from `registry.ts`'s `getConnectorInstanceByType`/`getStorageAdapter`. **Phase 1b/2 work is "fill these stub bodies in," not "create new files."** No `onedrive-permission-adapter.ts`, `onedrive-content-adapter.ts`, or migration-adapter file exists yet — net new for Phase 2, following the exact factory-function pattern of the three Google Drive adapter files (`create*Adapter(getAccessToken): I*Adapter`).

**Design decisions made with the user (2026-08-05):**
1. **SharePoint is a sub-mode of `ConnectorType.ONEDRIVE`, not a new enum value.** No Prisma migration. `Connector.workspaceRootSharedStorageId`/`workspaceRootSharedStorageName` (already generic, already used by Google's Shared Drive selection) hold the chosen SharePoint site's drive id/name. The connect UI presents **"Personal (OneDrive)" vs "Shared (SharePoint)"** as two options at connect time, deliberately mirroring the existing Google Drive Personal-Drive-vs-Shared-Drive picker UX the user already has in `google-drive-workspace-root.tsx`. `WorkspaceRootLocation.PERSONAL`/`SHARED` (existing Prisma enum) is reused as-is — `PERSONAL` = OneDrive, `SHARED` = SharePoint site, exactly parallel to Google's own use of these two values.
2. **Phase 3 uses `Sites.Selected`, not the already-granted `Sites.ReadWrite.All`.** Rationale (confirmed by reading `google-drive-connector.ts`'s Shared Drive handling): Google's `drive.file` scope is **not** actually broad — it grants zero access until the user explicitly picks a file/folder via Google's picker, even for Shared Drives (the code has a comment explaining the app literally cannot touch a Shared Drive folder it didn't create or the user didn't hand-pick). `Sites.ReadWrite.All` is categorically different — blanket access to every SharePoint site the user can reach, granted once, no per-site picker gate. `Sites.Selected` is the actual behavioral analog: zero access until a site is chosen in a picker UI, then one explicit `POST /sites/{siteId}/permissions` grant call for just that site — same "nothing until picked" model as Google. **Action required from the user**: add the `Sites.Selected` delegated permission in the Azure Portal (App registrations → Firma Connect → API permissions → Add a permission → Microsoft Graph → Delegated → `Sites.Selected`) before Phase 3 starts; `Sites.ReadWrite.All` can stay granted alongside it (unused) or be removed later — not urgent either way.
3. **Phase 5 builds the full connect UI now**, not a minimal connect-button-only slice — both `firm-drive-section.tsx` and `client-drive-section.tsx` get real OneDrive/SharePoint connect flows (button → OAuth popup → Personal/Shared picker → status display → disconnect), mirroring the existing Google Drive block in each file.

**New feature flag**: `MICROSOFT_CONNECTOR_ENABLED` (env var, server-only, defaults `false`) — gates the hardcoded-tile replacement in `firm-drive-section.tsx` and the new `app/api/connectors/onedrive/*` routes (routes 404/reject if flag is off, same fail-closed pattern as `MICROSOFT_SIGNIN_ENABLED`). Renamed from the originally-planned `ONEDRIVE_CONNECTOR_ENABLED` to match the `MICROSOFT_SIGNIN_ENABLED` naming convention (user request during implementation). This is a **separate flag from `MICROSOFT_SIGNIN_ENABLED`** — sign-in and the storage connector are independent features with independent readiness (sign-in is blocked on publisher verification; the connector isn't, since firm admins consenting to connect storage are a much smaller, more sophisticated audience than every new signup).

---

## Implementation status — 2026-08-05: Phases 1b, 2, 3, 5 built end-to-end

All four phases were implemented in one pass, directly on `dev`, flagged off by default via `MICROSOFT_CONNECTOR_ENABLED=false`. `tsc --noEmit` is clean across the whole project. **Not yet done: live testing** (no Azure `Sites.Selected` permission grant confirmed yet, no manual OAuth round-trip run) — see "Still needed" below before flipping the flag on anywhere.

**Files added:**
- `app/api/connectors/onedrive/route.ts` — initiate (personal/shared scope split) + status + test actions
- `app/api/connectors/onedrive/callback/route.ts` — token exchange, Graph `/me`, `storeConnection`, popup/redirect response, reconnect-same-account guard — mirrors Google's callback structure exactly
- `app/api/connectors/onedrive/sites/route.ts` — GET lists SharePoint sites (Graph `/sites?search=`); POST grants `Sites.Selected` access to the chosen site (`POST /sites/{id}/permissions`) then resolves and persists its drive as the shared workspace root
- `lib/connectors/onedrive-connector.ts` — filled in (was a stub): `storeConnection`, `refreshAccessToken` (marks `EXPIRED` on `invalid_grant`/`interaction_required`), `getAccessToken`, `getConnections`, `disconnectConnection`, `getFileMetadata`. New `OneDriveAuthError` class mirroring `GoogleDriveAuthError`.
- `lib/connectors/adapters/onedrive-adapter.ts` — filled in (was a stub): full `IConnectorStorageAdapter` against Graph, base URL resolved per-connection (`/me/drive` vs `/sites/{id}/drive`) via `workspaceRootLocation`/`workspaceRootSharedStorageId`
- `lib/connectors/adapters/onedrive-permission-adapter.ts` — new, full `IConnectorPermissionAdapter`. `commenter` role maps to Graph `read` (no native comment-only role — documented gap, not a bug). `deleteFile`'s `permanent` flag has no distinct effect (Graph always recycle-bins).
- `lib/connectors/adapters/onedrive-content-adapter.ts` — new, full `IConnectorContentAdapter`. `?format=pdf` handles Office→PDF natively (simpler than Drive — no shortcut/stub resolution needed). `setCopyRestricted` is an intentional no-op (Graph has no equivalent).
- `lib/onedrive-popup-oauth.ts` — new, mirrors `google-drive-popup-oauth.ts` exactly (postMessage type `onedrive_oauth`, reuses `isGoogleDriveOAuthPopupOriginAllowed` for origin-matching since the logic is provider-agnostic)
- `components/ui/onedrive-icon.tsx`, `components/ui/sharepoint-icon.tsx` — simple inline SVG marks (no external asset dependency, unlike Google's PNG product mark)

**Files changed:**
- `lib/config.ts` — added `getMicrosoftOAuthServerCredentials()`, `isMicrosoftConnectorEnabled()`, `config.onedrive.{clientId,clientSecret,redirectUri}`
- `lib/connectors/registry.ts` — `getPermissionAdapter`/`getContentAdapter` now return the new OneDrive adapters instead of `null` for `ConnectorType.ONEDRIVE`. `getMigrationAdapter` no longer throws for `ONEDRIVE` — new inline-constructed migration adapter (same no-separate-file pattern as Google's) added directly in the function, using Graph's server-side move (`PATCH parentReference`) and resolving `persistWorkspaceRootLocation`'s Personal/Shared split via the moved item's `parentReference.siteId`.
- `lib/actions/firms.ts` — `FirmConnectorRecord` gained a `type` field (was previously omitted — `getFirmConnectors` already queried across all types but never surfaced which); `disconnectFirmConnector`/`removeFirmConnector`/`renameFirmConnector` needed **no changes** — already fully generic across connector types.
- `components/connectors/firm-drive-section.tsx` — replaced the hardcoded disabled "OneDrive — Coming Soon" tile with a working connect button (gated by new `microsoftConnectorEnabled` prop) → Personal/Shared picker dialog → OAuth popup → (if Shared) site picker dialog → status. Google Drive connectors and OneDrive connectors now render as two separate card lists (filtered by `connector.type`) rather than one unified list, since OneDrive's card is deliberately simpler (no Switch Account/Rename/per-connector client-attach UI — those stayed Google-only, out of Phase 5's scope as discussed with the user).
- `components/connectors/client-drive-section.tsx` — now self-detects connector provider (tries Google status endpoint first, falls back to OneDrive's) and renders the matching icon/label. No connect flow of its own — unchanged "Manage in Firm Settings" redirect pattern.
- `app/(app)/d/f/[slug]/page.tsx` → `firm-clients-view.tsx` → `firm-settings-form.tsx` → `firm-drive-section.tsx` — `microsoftConnectorEnabled` prop threaded through all four layers from a single `process.env.MICROSOFT_CONNECTOR_ENABLED === 'true'` read in the server page component, same fail-closed pattern as `microsoftSignInEnabled`.
- `env.example`, `env.production.example` — added `MICROSOFT_CONNECTOR_ENABLED=false`

**Deliberate scope cut vs. the original Phase 5 write-up**: "full connect UI" was interpreted as connect/disconnect/remove/status for both firm and client level — not full parity with Google's richer card (Switch Account, Rename, per-connector client attach/detach). Those Google-only affordances exist because of Google-specific constraints (e.g. Shared Drive account-switching limitations) that don't have a direct OneDrive analog yet; adding them can be a follow-up if firm admins actually need them.

**Still needed before flipping `MICROSOFT_CONNECTOR_ENABLED=true` anywhere:**
1. Add the `Sites.Selected` delegated permission in the Azure Portal (confirmed via screenshot during this session that only `Sites.ReadWrite.All` was granted as of 2026-08-05) — `Sites.ReadWrite.All` can stay granted alongside it, unused.
2. A live end-to-end OAuth test — connect flow, Personal mode, Shared mode + site picker + `Sites.Selected` grant — has not been run yet. Everything above is code-reviewed and type-checked only.
3. Production Supabase/Azure redirect URI for `/api/connectors/onedrive/callback` should be double-checked against what's actually registered in Azure (Phase 1a's actual-results section lists `https://www.firma.bz/api/connectors/onedrive/callback` as already registered — should still hold, but wasn't re-verified this session).

---

### Phase 1b — Microsoft Graph OAuth code

**New env vars** (already captured in Phase 1a, just consumed here): `MICROSOFT_CLIENT_ID`, `MICROSOFT_CLIENT_SECRET`, `MICROSOFT_TENANT_ID`. Plus new: `ONEDRIVE_CONNECTOR_ENABLED`.

**New files** (mirror `app/api/connectors/google-drive/{route.ts,callback/route.ts}` exactly in structure):
- `app/api/connectors/onedrive/route.ts` — POST `action=initiate`. Build authorize URL against `https://login.microsoftonline.com/common/oauth2/v2.0/authorize` (use `common`, not the tenant id, matching Phase 1a step 7 — supports both personal and work/school accounts). Query params: `client_id`, `redirect_uri` (`{origin}/api/connectors/onedrive/callback`), `response_type=code`, `response_mode=query`, `scope` (space-joined — see scope table below), `state`, `prompt=consent` (force refresh token issuance, mirrors Google's `access_type=offline&prompt=consent`). Return 404/`{error:'not_enabled'}` if `ONEDRIVE_CONNECTOR_ENABLED !== 'true'`.
- `app/api/connectors/onedrive/callback/route.ts` — GET. Exchange `code` at `https://login.microsoftonline.com/common/oauth2/v2.0/token` (`grant_type=authorization_code`, `client_id`, `client_secret`, `code`, `redirect_uri`). Fetch `https://graph.microsoft.com/v1.0/me` for account identity (Graph's analog of Google's `userinfo` endpoint — gives `id`, `mail`/`userPrincipalName`, `displayName`). Call `oneDriveConnector.storeConnection(...)` (new method, mirrors `GoogleDriveConnector.storeConnection` signature — see Phase 2). Fire `AUDIT_EVENT.STORAGE_CONNECTOR_ATTACHED` with `meta({ provider: 'onedrive', ... })`, same pattern as Google's callback.

**State param**: reuse the exact same base64-JSON `state` shape and construction as Google's (`userId`, `organizationId`, `clientId?`, `next`, `flow`, `nonce` for popup flow, `replaceConnectorId?`, `friendlyName?`) — no new CSRF design needed, same non-cryptographic-but-code-exchange-backed model as the existing Google flow. Add one new field: `mode: 'personal' | 'shared'` (set from the connect-UI's Personal/Shared picker choice — see Phase 5), consumed by the callback to decide whether to also kick off the site-picker step (Phase 3) or finish immediately (Personal/OneDrive case).

**Scope table** (per Phase 1a step 5's scope-separation design, now finalized with the `Sites.Selected` decision):
| Flow | Scopes requested |
|---|---|
| Sign-in (`MICROSOFT_SIGNIN_ENABLED`, already shipped) | `openid profile email` |
| Connect OneDrive (Personal) | `Files.ReadWrite.All offline_access` |
| Connect SharePoint (Shared) | `Files.ReadWrite.All Sites.Selected offline_access` |

**Popup vs redirect response**: reuse Google's `popupHtml()` pattern verbatim (`postMessage({ type: 'onedrive_oauth', ok, error, connectionId, ... }, window.location.origin)` then `window.close()`) — origin-matching logic (callback's own origin, not `openerOrigin` from state) carries over unchanged, same reasoning as documented in Google's callback.

**Reconnect-same-account guard**: port the same email/externalAccountId mismatch check from Google's callback (lines 319-340) — compare Graph `/me`'s `id`/`mail` against the existing connector's stored `externalAccountId`/`accountEmail` when `replaceConnectorId` is present.

**Risk/impact**: Low regression risk — entirely new routes and env vars, zero existing code path touched, flagged off by default. A bug here blocks OneDrive connection attempts but cannot affect Google Drive users or any other part of the app.

---

### Phase 2 — Real adapter implementations (fill the existing stubs)

**`lib/connectors/onedrive-connector.ts`** — replace stub bodies in the existing `OneDriveConnector` class (keep the `getInstance()` singleton shape as-is):
- `async storeConnection(organizationId, userId, externalAccountId, name, accessToken, refreshToken, tokenExpiresAt, accountEmail?, clientId?, mode: 'personal'|'shared', sharedStorageId?, sharedStorageName?): Promise<Connector>` — mirror `GoogleDriveConnector.storeConnection`'s dedupe-on-`(type, userId, externalAccountId)`-then-upsert logic; set `type: ConnectorType.ONEDRIVE`, `workspaceRootLocation: mode === 'shared' ? 'SHARED' : 'PERSONAL'`, and `workspaceRootSharedStorageId`/`Name` when `mode === 'shared'`.
- `async refreshAccessToken(connectionId): Promise<string>` — POST `https://login.microsoftonline.com/common/oauth2/v2.0/token` with `grant_type=refresh_token`, `refresh_token` (decrypted via the existing Prisma extension — no new encryption code needed, `ENCRYPTED_FIELDS_MAP` in `lib/prisma.ts` already covers `connector.accessToken`/`refreshToken` generically for any `ConnectorType`). On `invalid_grant`/`interaction_required` error response, mark `Connector.status = EXPIRED` — same pattern Google's `refreshAccessToken` uses, per Phase 1a step 11's refresh-token guidance (90-day sliding window).
- `getAccessToken(connectionId): Promise<string | null>` — fetch `Connector`, check `tokenExpiresAt`, call `refreshAccessToken` if expired, return decrypted `accessTokenDecrypted`.
- `getConnections`/`disconnectConnection`/`removeConnection` — implement against the `Connector` model exactly as Google's equivalents do (no Google-specific logic in these; they're generic Prisma queries already, just currently stubbed for OneDrive).
- Remove the unused `ConnectorType` import noted in the stub (dead code cleanup, not required but free while touching this file).

**`lib/connectors/adapters/onedrive-adapter.ts`** (`IConnectorStorageAdapter`) — replace each `stub()` call with a real Graph API call. Base URL branches on mode: `https://graph.microsoft.com/v1.0/me/drive` (Personal) vs `https://graph.microsoft.com/v1.0/sites/{siteId}/drive` (Shared — `siteId` read from `Connector.workspaceRootSharedStorageId`).
- `listFolderChildren` → `GET /drive/items/{folderId}/children`
- `readFileContent` → `GET /drive/items/{fileId}/content`
- `writeFile`/`writeFileBinary` → `PUT /drive/items/{folderId}:/{fileName}:/content` (simple upload, <4MB; files above that need `createUploadSession` from the content adapter — Graph enforces this at the API level, not optional like Drive's resumable upload is)
- `createFolder`/`findOrCreateFolder` → `POST /drive/items/{parentId}/children` with `{ folder: {} }`, `@microsoft.graph.conflictBehavior: 'fail'` for find-or-create's existence check
- `getFileParent`/`getFolderName`/`fileExists` → `GET /drive/items/{id}` reading `parentReference`/`name`
- `search` → `GET /drive/root/search(q='{query}')`

**New `lib/connectors/adapters/onedrive-permission-adapter.ts`** (`createOneDrivePermissionAdapter(): IConnectorPermissionAdapter`, mirrors `google-drive-permission-adapter.ts`'s factory shape):
- `grantFolderPermission`/`grantFilePermission` → `POST /drive/items/{id}/invite` with `roles: [graphRole]`, `sendInvitation`, `recipients: [{email}]` — map `ConnectorRole` (`viewer|editor|commenter`) → Graph roles (`read|write` — **Graph has no native `commenter`/comment-only role**; map `commenter` → `read` and note this as a real UX gap vs Google, not silently drop it)
- `revokePermission`/`deleteFile` → `DELETE /drive/items/{id}/permissions/{permissionId}` / `DELETE /drive/items/{id}` (`deleteFile`'s `permanent` flag: Graph's delete always goes to the site/drive's recycle bin, there's no true hard-delete via this endpoint — document this as a Google-vs-Graph behavioral gap, not a bug, since the only current `permanent: true` caller was the now-superseded Drive-backed support attachments, per the Status block above)
- `listFilePermissions` → `GET /drive/items/{id}/permissions`
- `getFileMetadata` → `GET /drive/items/{id}` mapped to `ConnectorFileMetadata` (`driveId` field maps to the SharePoint site's drive id when in Shared mode, `null` for Personal — same semantic Google uses for Shared Drive membership)
- `downgradeFolderUserPermissionToReader` → `PATCH /drive/items/{id}/permissions/{permissionId}` with `roles: ['read']`
- `getEngagementFolderIds`/`trashFile`/`listFiles` → implement against the same Graph endpoints as above, matching Google's method contracts exactly (these are thin wrappers already generic in the interface)

**New `lib/connectors/adapters/onedrive-content-adapter.ts`** (`createOneDriveContentAdapter(): IConnectorContentAdapter`):
- `createFile` → `PUT /drive/items/{folderId}:/{fileName}:/content`
- `overwriteFileContent` → same endpoint, existing item id
- `createUploadSession` → `POST /drive/items/{folderId}:/{fileName}:/createUploadSession` — Graph's real resumable-upload API (required, not optional, above 4MB), returns `uploadUrl` directly usable as the interface's return shape
- `getRenderableContent`/`getPreviewableContent` → Graph's `?format=pdf` query param on `GET /drive/items/{id}/content` handles Office-format-to-PDF conversion natively (no `.gdoc`-style stub-file resolution needed — this is a case where Graph is actually simpler than Drive, since Graph has no shortcut/stub-file concept to resolve first)
- `setCopyRestricted` → Graph has no direct per-file copy-restriction equivalent to Drive's `copyRequiresWriterPermission`; implement as a no-op that resolves silently (interface explicitly allows this — "No-op ... for providers without an equivalent concept," `types.ts` docstring) and note it in the PR description as a known Graph capability gap, not a bug

**New migration-adapter wiring in `registry.ts`'s `getMigrationAdapter()`**: follow the same inline-construction pattern used for Google (no separate file) — `listTopLevelChildren`/`getFolderBreadcrumb` via the Graph endpoints above, `moveBatch` via `PATCH /drive/items/{id}` with new `parentReference: { id: newParentFolderId }` (Graph's server-side move, confirmed available), `persistWorkspaceRootLocation` writes `Connector.workspaceRootSharedStorageId` same as Google's version. Change `getMigrationAdapter` to no longer throw for `ONEDRIVE` (currently throws for any non-`GOOGLE_DRIVE` type) — this is the one existing-function behavior change in this phase, scoped precisely to adding the new branch.

**Risk/impact**: Low regression risk to existing users — new files/methods replacing stubs, `registry.ts` dispatch already branches by type so Google Drive is untouched except the one `getMigrationAdapter` branch addition. Primary risk is scoped to OneDrive-connected firms: an incorrect Graph mapping (permission role translation, especially the `commenter`→`read` downgrade, or folder-move semantics) could silently under/over-grant access or behave unexpectedly during workspace migration for those firms specifically.

---

### Phase 3 — SharePoint site selection

**Site picker step**: new UI step (Phase 5) triggered when the user picks "Shared (SharePoint)" at connect time — calls a new internal route, `app/api/connectors/onedrive/sites/route.ts` (GET, `?q=` optional search term), which proxies `GET https://graph.microsoft.com/v1.0/sites?search={q}` (or `/sites?search=*` for an initial unfiltered list) using the just-obtained access token, returning `{ id, name, webUrl }[]` for the picker UI — directly analogous to Google's `action: 'drives'` listing in the callback/connect flow that feeds `google-drive-workspace-root.tsx`'s picker.

**Per-site grant call** (required because of the `Sites.Selected` decision — this is genuinely new work not in the original plan, which assumed `Sites.ReadWrite.All`): after the user selects a site, call `POST https://graph.microsoft.com/v1.0/sites/{siteId}/permissions` with `{ roles: ['write'], grantedToIdentities: [{ application: { id: MICROSOFT_CLIENT_ID, displayName: 'Firma Connect' } }] }` **before** attempting any drive operation against that site — Graph will reject drive calls to a `Sites.Selected`-scoped app with 403 until this grant exists. Store the returned permission id in `Connector.settings.sitePermissionId` (untyped JSON blob, consistent with how other provider-specific bits are already stashed there per the Prisma model survey) in case it needs revoking later.

**Storage**: chosen site's Graph drive id → `Connector.workspaceRootSharedStorageId`; site display name → `workspaceRootSharedStorageName`; `workspaceRootLocation = 'SHARED'`. No schema migration — these fields are already fully generic (confirmed via Prisma schema read).

**Risk/impact**: Low-Medium regression risk — no schema migration, but this is the first non-Google write path into fields that have only ever held Google Shared Drive ids in production; any implicit Google-only assumption left in code reading those fields (UI labels, `workspaceRootLocation` derivation) would surface here first — grep for `workspaceRootSharedStorage` before starting this phase to catch any such assumption up front. Impact is contained to OneDrive/SharePoint-connected firms; a wrong site selection or a missing per-site grant risks either a broken connector (grant missing) or, in the worst case, pointing document sync at the wrong SharePoint site (cross-site exposure within that firm's own tenant, not cross-firm).

---

### Phase 5 — UI (full connect flow, both firm and client level)

**Files touched**: `components/connectors/firm-drive-section.tsx` (lines 695-708 today — hardcoded disabled tile) and `components/connectors/client-drive-section.tsx` (same pattern, gates client-level connectors). Both get the same treatment; build the firm-level flow first, then port the identical component/logic to the client-level file.

**Replace the hardcoded "Coming Soon" tile with**:
1. A "Connect OneDrive/SharePoint" button, gated by `ONEDRIVE_CONNECTOR_ENABLED` (passed down as a prop from the server component, same fail-closed pattern as `microsoftSignInEnabled` in `signin/page.tsx`/`signup/page.tsx` — **not** via the dead `getConnectorMeta`).
2. Clicking it opens a small **Personal vs Shared picker** (two options, "Personal (OneDrive)" / "Shared (SharePoint)" — mirrors the existing Google Personal-Drive/Shared-Drive choice already in this same file for the Google block) before kicking off the OAuth popup, so `mode` is known and included in the `state` param before Phase 1b's route is even called.
3. OAuth popup → `app/api/connectors/onedrive/route.ts` → Microsoft consent → callback → `postMessage` back (Phase 1b) → if `mode === 'shared'`, immediately show the site picker (Phase 3's `/sites` route) inside the same modal flow before considering the connector "fully connected"; if `mode === 'personal'`, connector is complete immediately, matching Google's flow shape.
4. Status/account display and disconnect button — reuse `FirmDriveSection`'s existing generic `connectors`/`loadConnectors`/`handleConnect` state and `getFirmConnectors(firmId)` call, which the code survey confirmed already returns OneDrive connectors once they exist (not filtered by type) — no changes needed there, just render a second card/row for `ConnectorType.ONEDRIVE` results alongside the existing Google Drive one.

**New components**: `onedrive-icon.tsx` / `sharepoint-icon.tsx` (static SVGs, mirror existing `google-drive-icon.tsx` shape).

**Risk/impact**: Low regression risk — new UI added alongside the existing Google Drive block in two files, not modifying Google's rendering path; the connect button and picker are net-new JSX, flagged off by default via `ONEDRIVE_CONNECTOR_ENABLED`. Impact of a bug is a broken OneDrive/SharePoint connect flow, visible only to firm admins who click the new button — zero impact on Google Drive connect/disconnect for any firm.

### Phase 6 — Testing
- Mirror existing adapter test suites (`google-drive-adapter.test.ts` pattern) for OneDrive
- Migration/permission/content adapter tests
- **Risk/impact**: No regression risk — test-only changes, no production code paths modified. This phase's value is risk *reduction*: it's the safety net that should catch Phase 2/3 defects before they reach a real OneDrive-connected firm.

---

# Phase 0: Abstract Document-Lifecycle Drive Coupling

## Context

Before OneDrive/SharePoint storage can be added, every code path that touches Drive must go through the provider adapter abstraction — otherwise OneDrive support would only cover folder structure (Pockett) and miss real user-facing features. An audit found three areas that bypass the abstraction:

1. **Document preview** — raw Drive fetches for metadata, shortcut/stub resolution, and PDF export
2. **Support ticket attachments** — raw Drive fetches for upload/resumable-upload/delete/download
3. **Sharing regrant** — raw `GoogleDriveConnector` calls for per-file permission grant/list/revoke, PDF export, and content overwrite

A fourth area, **activity/badge computation** (`driveactivity.query`, risk/stale/sensitive badges), is **explicitly deferred** — Microsoft Graph has no 1:1 equivalent API, and abstracting it now would be speculative. It stays hardcoded to `GoogleDriveConnector` and is out of scope here.

This phase does **not** implement OneDrive/Graph logic. It only extends the interfaces and refactors the Google side to route through them, so a future OneDrive adapter can implement the same surface. Google Drive behavior must remain byte-for-byte identical after this refactor — it is a pure abstraction pass, not a feature change.

## Decisions

- **Role vocabulary rename**: `IConnectorPermissionAdapter`'s `'reader' | 'writer' | 'commenter'` → generic `'viewer' | 'editor' | 'commenter'`. Blast radius is small — confirmed via grep, only 3 real call sites:
  - `lib/grant-engagement-drive-folder-access.ts:47`
  - `lib/inngest/functions.ts:1046`
  - `lib/connectors/adapters/google-drive-permission-adapter.ts:13-14` (pass-through wrapper, maps generic role → Drive's `role` string for `GoogleDriveConnector.grantFolderPermission`)
  - Plus the interface declaration (`types.ts:116`) and `GoogleDriveConnector.grantFolderPermission`/`downgradeFolderUserPermissionToReader` signatures (`google-drive-connector.ts:3178`, `:3230`).
- **Activity/badges**: out of scope, untouched.
- **Google-only quirks with no Graph equivalent** (`.gdoc`/`.gsheet`/`.gslides` stub-file resolution, Drive shortcut `targetId` indirection): kept entirely inside the Google adapter implementation, invisible to the interface. The interface exposes only the *outcome* (resolved renderable content), not the resolution mechanism.

## New interface surface

Add to `frontend/lib/connectors/types.ts`. Two additions to `IConnectorPermissionAdapter` (per-file, not per-folder, permission ops — the existing methods are folder-oriented) and a new `IConnectorContentAdapter` for content mutation/export operations that don't belong on the folder-structure-oriented `IConnectorStorageAdapter`.

```typescript
// --- IConnectorPermissionAdapter additions ---
// Generic role vocabulary (renamed from Drive-specific 'reader'|'writer'|'commenter')
export type ConnectorRole = 'viewer' | 'editor' | 'commenter'

grantFolderPermission(connectionId: string, folderId: string, email: string, role: ConnectorRole): Promise<string | null>  // signature updated in place
downgradeFolderUserPermissionToReader(...) // unchanged signature, consider renaming to downgradeFolderUserPermissionToViewer in same pass since it's role-vocabulary-adjacent

grantFilePermission(connectionId: string, fileId: string, email: string, role: ConnectorRole, opts?: { notify?: boolean }): Promise<string | null>
listFilePermissions(connectionId: string, fileId: string): Promise<Array<{ id: string; email: string | null; role: ConnectorRole }>>
deleteFile(connectionId: string, fileId: string, opts?: { permanent?: boolean }): Promise<void>  // supersedes trashFile; permanent:true = hard delete (support attachments), false/omitted = trash (existing trashFile behavior)

// --- New: IConnectorContentAdapter (file-content lifecycle, separate from folder-structure-oriented IConnectorStorageAdapter) ---
export interface IConnectorContentAdapter {
  /** Create a new file with binary content in a folder, returning its id. Supersedes writeFileBinary for callers that need the id back. */
  createFile(connectionId: string, folderId: string, fileName: string, content: Buffer, mimeType: string): Promise<{ id: string }>

  /** Overwrite an existing file's content in place. */
  overwriteFileContent(connectionId: string, fileId: string, content: Buffer, mimeType?: string): Promise<void>

  /** Begin a resumable/chunked upload session for large files. Returns an opaque session handle + upload URL. */
  createUploadSession(connectionId: string, folderId: string, fileName: string, mimeType: string, sizeBytes: number): Promise<{ uploadUrl: string; sessionId: string }>

  /**
   * Get renderable content for a file: either its native bytes or a PDF export.
   * Hides all provider-specific resolution (Drive shortcuts, .gdoc/.gsheet stubs, Workspace-mimetype export links,
   * copy-convert-export dance) behind one call. Google-only quirks live entirely inside the Google implementation.
   */
  getRenderableContent(connectionId: string, fileId: string, format: 'native' | 'pdf'): Promise<{ stream: ReadableStream | Buffer; mimeType: string; fileName: string; size?: number }>

  /**
   * Toggle copy/download restriction on a file, if the provider supports it.
   * No-op (resolve silently) for providers without an equivalent concept.
   */
  setCopyRestricted(connectionId: string, fileId: string, restricted: boolean): Promise<void>
}
```

**Design notes:**
- `getRenderableContent` unifies today's two divergent conversion paths (preview route's `exportLinks`/`exportFileToPdf` → PDF, and support download's `downloadFile` → Office-format export) into one method with a `format` parameter. The Google implementation internally still branches on Workspace mimetypes; callers no longer need to know that.
- `createUploadSession` models Drive's resumable-upload flow abstractly enough to also fit Graph's `createUploadSession` (PUT chunks) later, without committing to identical wire semantics now.
- `deleteFile` replaces `trashFile` in the permission adapter with an explicit `permanent` flag, covering both today's soft-delete (existing engagement document trash) and hard-delete (support attachments) call sites. `trashFile` callers migrate to `deleteFile(..., { permanent: false })`.
- `IConnectorContentAdapter` is deliberately separate from `IConnectorPermissionAdapter` and `IConnectorStorageAdapter` — content mutation/export is a distinct concern from folder structure and sharing. Registry gets a new `getContentAdapter(connectionId)` accessor, following the exact pattern of `getPermissionAdapter`/`getStorageAdapter` in `lib/connectors/registry.ts`.
- The regrant route's "share PDF only" branch (creates a sibling PDF file, grants permission on it) is **not** re-architected in this phase — it's refactored to call `createFile` + `grantFilePermission` instead of raw Drive calls, but the sibling-file *pattern* itself is preserved as-is to keep behavior identical. Revisiting whether Graph's direct `?format=pdf` conversion makes the sibling-file pattern unnecessary is future OneDrive-phase work, not this phase.

## Migration sequence

Refactor in increasing order of regression risk, each as its own PR gated by the listed verification:

### Step 1 — Interface + registry additions (no behavior change) — ✅ Done
- Add `ConnectorRole`, `grantFilePermission`, `listFilePermissions`, `deleteFile` to `IConnectorPermissionAdapter` in `types.ts`
- Add new `IConnectorContentAdapter` to `types.ts`
- Add `getContentAdapter(connectionId)` to `registry.ts`
- Implement all new methods in `google-drive-permission-adapter.ts` (permission additions) and a new `google-drive-content-adapter.ts` (content adapter) — both as thin wrappers delegating to existing `GoogleDriveConnector` methods (`grantFilePermission` → `google-drive-connector.ts:3271`, `listFilePermissions` → `:3089`, `exportFileToPdf` → `:3983`, `overwriteFileContent` → `:4130`, `uploadNewFile` → `:4078`, `getResumableUploadUrl` → `:963`)
- Rename role vocabulary at the 3 call sites identified above + interface + `GoogleDriveConnector` signatures
- No route/UI changes yet. Verify: `tsc --noEmit`, existing `google-drive-permission-adapter.test.ts` pattern extended with new method tests.
- **Regression risk: None** — purely additive interface/adapter code, no existing call site's runtime behavior changed (only the 3 role-string call sites were touched, and those are type-level renames with identical semantics). Verified via `tsc --noEmit` (clean) and full test suite (54/54 passing, 7 new).
- **Impact if it breaks**: None — nothing yet reads these new methods; a defect here would only surface as a type error or unit test failure, not a runtime issue for any user.

### Step 2 — Support ticket attachments — ⚠️ SUPERSEDED, see status block above

Originally implemented as written below (Drive-backed, via `contentAdapter`), but **subsequently redesigned to DB-blob storage entirely off Drive** after a real client-less-ticket bug was found in manual QA — see "Status as of 2026-07-27" point 1 near the top of this doc for the current design. The steps below are kept for historical record only; do not use them as current truth.

<details>
<summary>Original (superseded) plan</summary>

- `upload-attachment/route.ts`: replace raw `POST .../files` + `PATCH .../upload/...` with `contentAdapter.createFile(...)`
- `prepare-upload/route.ts`: replace `googleDriveConnector.getResumableUploadUrl` with `contentAdapter.createUploadSession(...)`
- `attachments/[driveFileId]/delete/route.ts`: replace raw `DELETE` with `permissionAdapter.deleteFile(connectorId, id, { permanent: true })`
- `attachments/[driveFileId]/download/route.ts`: replace `googleDriveConnector.downloadFile` with `contentAdapter.getRenderableContent(connectorId, id, 'native')`
- Leave `driveFileId` naming in `CustomerRequest.attachments` JSON and the `[driveFileId]` route segment as-is — it's a JSON column (no schema migration needed) and a route param name; renaming is cosmetic and not required for OneDrive to work. Note it as a known naming inconsistency, don't fix it in this phase.
- Verify: upload/delete/download a support ticket attachment end-to-end in the running app; confirm stored `driveFileId` still resolves.
- **Regression risk: Low** — narrow, non-critical-path feature (internal support ticketing, not client-facing document workflows); small number of call sites, each swapping one raw fetch for one adapter call with an equivalent existing `GoogleDriveConnector` method underneath (no new logic introduced).
- **Impact if it breaks**: Support agents/users can't attach or retrieve files on a ticket. Annoying but does not touch engagement documents, client data, or sharing — fully isolated to the support subsystem.

</details>

### Step 3 — Document preview — ✅ Done (code)
- Replace all raw fetches in `preview/route.ts` with `contentAdapter.getRenderableContent(connectorId, resolvedId, format)`, where `format` is `'pdf'` for the existing PDF-preview path and `'native'` for raw download
- Google's implementation of `getRenderableContent` internally keeps the shortcut-resolution and `.gdoc`/`.gsheet` stub-detection logic exactly as it exists today in the route — just moved into `google-drive-content-adapter.ts` (or kept in `GoogleDriveConnector` and called from the adapter) rather than inline in the route handler
- Verify: preview a native Google Doc, a Google Sheet, an uploaded Office file, a shortcut, and a `.gdoc` stub file — all five cases must render identically to current behavior
- **Regression risk: Medium** — user-facing and frequently used (every document preview goes through this route); the route currently branches on five distinct cases (native Workspace doc, uploaded Office file needing PDF conversion, Drive shortcut, `.gdoc`/`.gsheet` stub, already-PDF), all of which must be preserved exactly when moved behind `getRenderableContent`. Highest chance of an edge case slipping through of any step except Step 4.
- **Impact if it breaks**: Every user in the app loses the ability to preview one or more document types inline — a highly visible, daily-use feature. Files remain safely stored in Drive (no data loss), but the app becomes noticeably degraded until fixed.

### Step 4 — Sharing regrant — ✅ Done (code)
- `sharing/regrant/route.ts`: replace direct `GoogleDriveConnector.getInstance()` calls with adapter calls:
  - `drive.revokePermission` → `permissionAdapter.revokePermission`
  - `drive.exportFileToPdf` → `contentAdapter.getRenderableContent(..., 'pdf')`
  - `drive.overwriteFileContent` → `contentAdapter.overwriteFileContent`
  - `drive.getFileMetadata` → `permissionAdapter.getFileMetadata` (already returns `ConnectorFileMetadata` — confirm no field the route needs is missing; extend `ConnectorFileMetadata` if so)
  - `drive.uploadNewFile` → `contentAdapter.createFile`
  - `drive.grantFilePermission` → `permissionAdapter.grantFilePermission`
  - `drive.listFilePermissions` → `permissionAdapter.listFilePermissions`
  - `patchFileProperties`/`copyRequiresWriterPermission` → `permissionAdapter.setCopyRestricted` (Google implementation maps to the existing Drive property call; behavior unchanged)
- The Drive-specific notification UX (`sendNotificationEmail`, `emailMessage`, the hardcoded "Google Drive requires a one-time email verification" message at regrant route ~L150) stays as Google-specific copy/params inside the Google permission adapter implementation — not exposed on the interface. Update the user-facing message only if it becomes provider-conditional later; leave as-is now.
- Verify: full regrant flow for both "share original" and "share PDF only" branches, confirm permission emails still send, confirm revoke still works, confirm copy-restriction toggle still applies.
- **Regression risk: Highest of the four steps** — touches live permission grants directly; 8 distinct Drive calls are being swapped in one route, including the copy-restriction toggle and the "share PDF only" branch's create-sibling-file-then-grant sequence, so there's more surface for a subtle behavioral mismatch than any other step.
- **Impact if it breaks**: The most severe of the four — a defect here could over-grant access (a security/confidentiality incident: an external collaborator or guest sees a file they shouldn't) or under-grant/fail-silently (a client-facing share appears to succeed but the recipient can't actually open the file, undermining trust in the sharing feature). Because this is the highest-impact step, it should be the last one shipped and get the most thorough manual verification pass before merge.

## Files touched (representative, not exhaustive per step)

- `frontend/lib/connectors/types.ts` — interface additions
- `frontend/lib/connectors/registry.ts` — `getContentAdapter` accessor
- `frontend/lib/connectors/adapters/google-drive-permission-adapter.ts` — new method implementations
- `frontend/lib/connectors/adapters/google-drive-content-adapter.ts` — new file
- `frontend/lib/grant-engagement-drive-folder-access.ts`, `frontend/lib/inngest/functions.ts` — role-name-only edits (call sites already use the adapter)
- `frontend/app/api/support/requests/[ticketNumber]/upload-attachment/route.ts`
- `frontend/app/api/support/attachments/prepare-upload/route.ts`
- `frontend/app/api/support/requests/[ticketNumber]/attachments/[driveFileId]/{delete,download}/route.ts`
- `frontend/app/api/projects/[projectId]/documents/[documentId]/preview/route.ts`
- `frontend/app/api/projects/[projectId]/documents/[documentId]/sharing/regrant/route.ts`

## Implementation deviations from the original interface sketch

Two additions emerged during implementation that weren't in the original design and are worth recording:

- **`ConnectorContentError`** (new exported class in `types.ts`) — `IConnectorContentAdapter` methods throw this with a `code: 'not_found' | 'forbidden' | 'unsupported'` so route handlers can map failures to specific HTTP responses without inspecting provider-specific error shapes. Needed once it became clear the preview route's granular 404/403/"unsupported" responses had to be preserved exactly.
- **`getPreviewableContent`** (new method on `IConnectorContentAdapter`, separate from `getRenderableContent`) — the preview route's format selection isn't a literal "give me native or give me PDF" choice; it dynamically picks raw-bytes-passthrough (PDF/image) vs PDF-conversion (Office/Workspace docs) based on mimetype, on top of resolving shortcuts/stubs first. Reusing `getRenderableContent(format: 'pdf')` for this would have overloaded what `'pdf'` means (Step 4's regrant flow uses it to mean "always force-convert," a different intent). Kept as a separate method so each interface method's contract stays literal.

## Verification (end-to-end, after all 4 steps)

1. `tsc --noEmit` clean after each step — ✅ verified, clean throughout
2. Existing tests: `frontend/lib/connectors/registry.test.ts`, `google-drive-adapter.test.ts`, `google-drive-permission-adapter.test.ts`, `google-drive-content-adapter.test.ts` — ✅ 85/85 passing (extended with new-method coverage per step)
3. **Manual pass in the running app — not yet done, recommended before merge:** support attachment upload/download/delete, document preview across all format branches (native doc, sheet, office file, shortcut, `.gdoc` stub), full sharing regrant flow including PDF-only sharing and watermarking
4. Confirm no remaining direct `googleapis.com` fetches or `GoogleDriveConnector.getInstance()` calls in the four target route files — ✅ verified via grep, zero matches in all four routes; badges/activity code in `google-drive-connector.ts` remains untouched as intended
