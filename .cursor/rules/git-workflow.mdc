---
description: Git safety
globs: .git/**/*
---

- No `git commit` / `git push` without explicit user instruction; confirm before those commands.
- Phrases like “commit and push” or “fix this and push” count as explicit instruction.
