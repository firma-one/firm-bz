---
description: Vercel build wiring
globs: vercel.json, package.json
---

- Vercel **Build Command** = `npm run build` (so `postbuild`/migrations run). Do not set Vercel to raw `next build` only.
- In `package.json`, `"build": "next build --webpack"` when Turbopack/PostCSS breaks the default pipeline.
